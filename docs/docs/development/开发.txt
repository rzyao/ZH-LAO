你现在负责 ZH-LAO  的 **Platform Domain Implementation**。

这是一个纯执行开发会话。

不要重新设计 Platform 产品方案。

设计权威来自上一会话已经冻结并进入仓库的 Platform canonical 文档。

执行流程：

```text
Read Frozen Design
→ Execute PLATFORM_IMPLEMENTATION_PLAN
→ Test
→ Audit
→ Report
→ Exit Gate
→ Freeze Platform
→ STOP
```

---

# 一、进入条件

首先检查仓库：

```text
PLATFORM_DESIGN_GATE = PASS
```

并读取实际 canonical 文档。

至少包括：

```text
PLATFORM_IMPLEMENTATION_PLAN.md
PLATFORM_USE_CASES.md
PLATFORM_CONFIG_CONTRACTS.md
PLATFORM_API.md
PLATFORM_DESIGN_AUDIT.md
```

实际路径以仓库为准。

如果：

```text
PLATFORM_DESIGN_GATE != PASS
```

立即停止：

```text
PLATFORM_IMPLEMENTATION = BLOCKED
```

不要自行补设计然后继续。

---

# 二、已冻结基础

必须保持：

```text
PostgreSQL  Baseline = PASS
Application Foundation = PASS
Identity Domain = FROZEN
```

Platform 只能依赖：

```text
Foundation
Identity public contract（仅设计确实需要时）
```

不得 import Identity internal。

不得反向修改 Identity。

---

# 三、Platform frozen DB

只允许 6 张表：

```text
platform.feature_flags
platform.feature_flag_overrides
platform.runtime_configs
platform.app_versions
platform.announcements
platform.regions
```

读取：

```text
database/migrations/0300_platform.sql
```

作为最高物理权威。

禁止：

```text
新增表
删除表
修改 frozen migration
新增 Platform migration
```

如果实现与 frozen DB 冲突：

```text
DATABASE_CONTRACT_CONFLICT
```

停止该路径并报告。

---

# 四、目录架构

实现到：

```text
apps/backend/src/modules/platform/
├── domain/
├── application/
├── infrastructure/
├── http/
└── public/
```

遵守现有 Foundation module boundary。

跨 Domain 只能：

```text
other-domain/public
```

禁止直接：

```text
other-domain/application
other-domain/infrastructure
```

---

# 五、Repository 规则

Platform repository：

```text
SQL ownership = platform.*
```

不得 SQL：

```text
identity.*
operations.*
content.*
learning.*
social.*
chat.*
commerce.*
rewards.*
trust.*
audio.*
```

Shared infrastructure 只有冻结设计明确需要时通过 Foundation contract 使用。

不得新建 Platform Pool。

必须复用：

```text
Foundation DatabaseExecutor
Foundation TransactionManager
```

---

# 六、严格按计划执行

读取：

```text
PLATFORM_IMPLEMENTATION_PLAN.md
```

按 PLT-xx 顺序连续执行。

不要跳步骤。

每一阶段：

```text
Implement
→ Unit Test
→ PostgreSQL Integration
→ Architecture Audit
→ Gate
```

PASS 后进入下一项。

如果发现：

```text
BLOCKER
HIGH
```

属于 Platform 当前范围：

直接修复并重新测试。

如果需要改变 frozen product/design contract：

停止并标记：

```text
DESIGN_CONTRACT_CONFLICT
```

不要自行改设计语义。

---

# 七、Feature Flag Implementation

严格实现冻结 evaluation algorithm。

必须覆盖：

```text
default flag state
status semantics
override resolution
override fallback
retired/inactive behavior
missing flag behavior
region/scope behavior
```

完全按 `PLATFORM_CONFIG_CONTRACTS.md`。

不要自己添加 scope。

不要添加 DB 不支持的：

```text
user override
percentage rollout
AB bucket
device targeting
```

除非冻结设计明确存在。

---

# 八、Feature Flag Override

必须依赖 DB 当前唯一约束 / partial unique。

管理操作必须保证：

```text
set/update deterministic
remove idempotent if contract says so
```

并发时不能产生两个 canonical override。

需要真实 PostgreSQL race tests。

---

# 九、Runtime Config

实现冻结 key/value contract。

不得让 Runtime Config 变成：

```text
arbitrary business settings dump
```

必须按照冻结的：

```text
key rules
visibility
value parsing
missing behavior
validation
```

实现。

如果需要 typed registry：

按设计实现 application-level registry。

不要加数据库表。

---

# 十、App Version

严格实现冻结版本判断。

测试：

```text
current supported
optional update
required update
latest
minimum supported
unknown platform/version
```

版本比较必须完全符合冻结规则。

不要使用字符串直接：

```text
"2.10" < "2.9"
```

这种错误比较。

---

# 十一、Announcement

实现冻结：

```text
active window
status
region
language/locale
priority/order
```

只有 schema 真实支持的维度。

不实现：

```text
push
email
SMS
chat message
```

---

# 十二、Region

实现 Platform canonical region read/manage semantics。

禁止对：

```text
identity.basic_profiles
```

建立 physical FK。

跨 Domain validation 只通过 public contract / application contract。

如果 Identity 当前不需要调用 Platform：

不要提前耦合。

---

# 十三、Public Contract

实现并冻结：

```text
modules/platform/public
```

其他 Domain 可依赖这里。

禁止导出：

```text
repositories
DB records
DatabaseExecutor
TransactionManager
internal PK
SQL adapter
```

Architecture tests 必须证明：

```text
other domain -> platform/public = allowed
other domain -> platform/internal = forbidden
```

---

# 十四、HTTP/API

严格实现 `PLATFORM_API.md`。

HTTP adapter：

```text
validation
auth
use case invocation
presentation
```

禁止：

```text
SQL
direct repository access
business rules
```

必须使用 Zod strict schema。

Unknown fields：

```text
rejected
```

Public response：

```text
internal BIGINT = 0
```

---

# 十五、Admin / Operator Integration

如果冻结设计中 Management API 需要 operator permission，但 Operations 尚未实现：

不要发明临时管理员系统。

按照冻结方案处理：

```text
route deferred
or protected contract placeholder
or integration pending
```

具体以设计文档为准。

不得：

```text
hardcoded admin token
special query param secret
temporary password
```

---

# 十六、Outbox

完全按冻结 Design Decision。

如果：

```text
Platform V1 Outbox = none
```

不要因为 Foundation 有 Outbox 就新增 event。

如果有 frozen events：

必须使用：

```text
infrastructure.system_outbox_events
```

并与 Domain write 同 transaction。

不得建 Platform outbox table。

---

# 十七、Cache

如果设计裁决：

```text
No cache
```

就直接 PostgreSQL read。

如果设计裁决：

```text
in-process cache
```

严格实现 bounded TTL / invalidation。

禁止：

```text
Redis
Memcached
Kafka
```

除非 frozen design 已明确，否则本阶段一律不得引入。

---

# 十八、Error Model

复用 Foundation：

```text
AppError
HTTP error envelope
```

不要创建第二套 Platform errors infrastructure。

Domain error code 应稳定，例如：

```text
FEATURE_FLAG_NOT_FOUND
INVALID_FEATURE_FLAG_STATE
RUNTIME_CONFIG_NOT_FOUND
INVALID_APP_VERSION
REGION_NOT_FOUND
```

具体以冻结 API 文档为准。

---

# 十九、安全审计

检查：

```text
mass assignment
unknown fields
IDOR where relevant
admin management authorization boundary
config secret leakage
internal PK leakage
SQL ownership
error leakage
```

Platform Runtime Config 必须特别检查：

```text
secret
credential
token
private backend config
```

不得因为在 runtime_configs 就通过 public API 暴露。

---

# 二十、并发审计

至少真实 PostgreSQL 测试：

```text
Feature Flag concurrent update
Override concurrent set
Override set vs remove
Runtime Config concurrent update
Announcement activation/update
Region status update
App Version policy update
```

只测试冻结设计实际需要的竞争场景。

验证最终 invariant，不依赖窄毫秒 timeout。

---

# 二十一、DB Freeze 审计

最终必须：

```text
Frozen migration changes = 0
```

Fresh validation：

```text
Fresh migrations = 17
Second run = 0
Database audit = PASS
```

确认：

```text
Cross-domain physical FK = 0
Logical UUID violations = 0
```

---

# 二十二、Full Regression

Platform 开发结束后运行：

## Backend

```text
typecheck
lint
architecture audit
unit
build
integration
```

## Database

```text
database tests
fresh migration
repeat no-op
audit
```

## Foundation

确保 Foundation regression 没坏。

## Identity

Identity frozen regression 必须继续 PASS。

至少保证现有 Identity unit/integration/security/race 没有回归。

## Docs

```text
VitePress build = PASS
```

如果当前 CI 已包含 Admin/Docs：

也必须保持全绿。

---

# 二十三、Implementation Report

最终生成：

```text
PLATFORM_IMPLEMENTATION_REPORT.md
```

并根据 Implementation Plan 生成阶段报告。

最终报告至少包含：

```text
Scope
6-table mapping
Implemented Use Cases
Feature Flag contract
Runtime Config contract
App Version contract
Announcement contract
Region contract
Public contract
HTTP endpoints
Security
Concurrency
Database
Tests
Deferred
Known limitations
Findings
Gate
```

---

# 二十四、Final Audit

最后做独立 Platform Final Audit。

分类：

```text
BLOCKER
HIGH
MEDIUM
LOW
```

重点检查：

```text
6 frozen tables unchanged
required use cases complete
API matches frozen contract
public contract no internal leak
cross-domain SQL = 0
HTTP SQL = 0
HTTP repository access = 0
no Redis/Kafka
no accidental generic config dump
no Operations ownership duplication
no Identity region ownership duplication
no Notification duplication
```

只有：

```text
BLOCKER = 0
HIGH = 0
```

以及完整测试 PASS，才能：

```text
PLATFORM_IMPLEMENTATION = COMPLETE
PLATFORM_GATE = PASS
PLATFORM_DOMAIN = FROZEN
```

---

# 二十五、更新 DEVELOPMENT_PROGRESS

Gate PASS 后：

```text
Platform = COMPLETE
Gate = PASS
完成日期 = 实际日期
```

添加：

```text
Implementation Plan
Design docs
Audit
Implementation Report
```

链接。

保留真实历史。

---

# 二十六、最终必须 STOP

即使 Platform PASS：

不要自动进入：

```text
Operations
Content
Learning
Audio
Social
Chat
Commerce
Rewards
Trust
```

只输出最终 Platform 结果。

---

# 二十七、最终汇报格式

```text
Platform Implementation Result

PLT-xx = COMPLETE / BLOCKED
...

Database:
Frozen tables = 6
Frozen migration changes = 0
Fresh migrations = 17
Second run = 0
Database audit = PASS
Cross-domain SQL = 0

Architecture:
Platform DB Pool = 0
Foundation TransactionManager reused = YES
HTTP SQL = 0
HTTP Repository access = 0
Public contract internal exposure = 0

Feature Flags = PASS
Runtime Config = PASS
App Version = PASS
Announcements = PASS
Regions = PASS

Security = PASS
Concurrency = PASS

Tests:
Unit = ?
Integration = ?
HTTP = ?
E2E = ?
Security = ?
Race = ?

Full Backend Regression = PASS
Identity Regression = PASS
Foundation Regression = PASS
Docs Build = PASS

Findings:
BLOCKER = ?
HIGH = ?
MEDIUM = ?
LOW = ?

PLATFORM_DESIGN_GATE = PASS
PLATFORM_IMPLEMENTATION = COMPLETE / IN_PROGRESS
PLATFORM_GATE = PASS / FAIL
PLATFORM_DOMAIN = FROZEN / NOT_FROZEN
```

如果最终：

```text
BLOCKER = 0
HIGH = 0
```

且所有 Gate PASS：

冻结 Platform，然后立即停止。

**绝对不要自动进入 Operations。**