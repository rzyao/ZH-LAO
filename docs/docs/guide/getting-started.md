# 阅读与维护文档

## 阅读顺序

1. 从仓库文档根目录的 `PROJECT.md` 了解当前阶段和文档地图。
2. 阅读与任务相关的产品、架构或领域文档。
3. 查看文档状态：`baseline` 与 `frozen` 是当前实现依据；`designing` 与 `deferred` 必须等待主架构会话补充。
4. 对重要历史取舍查阅 [ADR](../adr/README.md)。

## 更新规则

主架构会话确认新的产品、领域或数据库结论后，更新对应唯一事实源，并同步 PROJECT 的状态表和必要的 ADR。不要另建 `final`、`v2` 等平行版本文档。
