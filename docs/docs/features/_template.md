---
id: feature-<slug>
title: <功能名称>
status: 规划中
owner: <团队或负责人>
user_visible: true
last_updated: YYYY-MM-DD
related:
  - <相关 Feature / Architecture / ADR 路径>
---

# <功能名称>

> 本文档是此功能的唯一当前事实源，同时服务用户视角和开发视角。

## 1. 功能概述

**一句话：** `<功能解决什么问题>`

**目标用户：** `<谁使用它>`

**范围：** `<包含什么；不包含什么>`

## 2. 用户视角

### 用户价值

`<用户获得的结果或价值>`

### 使用流程

1. `<步骤>`
2. `<步骤>`
3. `<步骤>`

### 页面与交互

- **入口：** `<入口位置>`
- **主要区域：** `<页面区域>`
- **主要操作：** `<操作与反馈>`

### 使用限制与异常提示

- `<限制、权限或配额>`
- `<失败时用户看到什么>`

## 3. 功能规则

### 规则 1：<名称>

`<规则、边界条件与示例>`

## 4. 开发视角

### 组件与职责

| 组件 | 职责 |
| --- | --- |
| `<组件>` | `<职责>` |

### 核心流程

```text
<触发事件>
  ↓
<处理步骤>
  ↓
<结果>
```

### 接口契约

| 接口 / 事件 | 说明 | 权限 / 调用方 |
| --- | --- | --- |
| `<待填写>` | `<待填写>` | `<待填写>` |

### 数据与状态

- `<核心实体、字段、状态或存储位置>`

### 技术约束与依赖

- `<依赖的服务、性能、安全或兼容性约束>`

## 5. 验收标准

- [ ] `<可验证的用户结果>`
- [ ] `<可验证的规则或异常场景>`

## 6. 关联资料

- Architecture：`<路径>`
- ADR：`<路径>`
- Guide：`<路径>`
