# Plan

No frozen migration is changed. Add migration 1280 because the frozen `contents` CHECK cannot represent the approved `zh_syllable` identity. Implement Content public query service plus PostgreSQL reader; replace repository SQL with an application port invoked by publish.
