# 实施计划交接摘要

## 关键决策

- 只改 `apps/admin` 的局部表现层，不新建组件库、不改 API/数据库/权限。
- 主要路径为 `lo-letter-page.tsx`；状态呈现调整落在 `lo-letter-table.tsx`。
- 使用现有 CSS 变量及 UI 组件，保障深色主题和焦点状态。

## 产物

- `../plan.md`：Express 内联实施计划。

## 风险与实施提示

- 保留 URL 搜索、分页、排序、行选择和任务状态机的所有现有回调。
- 不增加“新建字母”入口；它不属于当前 `/content/lo/letters` 的已确认能力。
- 先更新/扩展页面状态测试，再实施视觉调整；完成后运行 admin 的类型检查、lint、测试和构建。

## 既有经验

无：本快速轨道未运行研究阶段，因此没有可转交的研究经验。
