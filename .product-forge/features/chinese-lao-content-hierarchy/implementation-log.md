# 实施记录：中老语言内容管理

## 检查点一——后台导航与路由骨架

状态：通过

- 已登记内容概览以及 11 个中文、老挝语类别和审核发布路由。
- 已建立相互独立的类别页面；老挝语字母继续使用现有真实管理页面。
- 已扩展侧边栏映射，使远程菜单与安全回退菜单均按“内容管理 → 中文内容/老挝语内容 → 内容类别”显示。
- 已新增前向迁移 `1300_content_language_navigation.sql`，冻结迁移 `1270_platform_menus.sql` 保持不变。
- 后端菜单白名单已同步全部新增路由键。
- 已覆盖路由登记、菜单转换、侧边栏渲染和页面路由测试。

验证结果：

- 后台类型检查：通过；
- 后端类型检查和迁移清单检查：通过；
- 后台针对性测试：23 项通过；
- 后端菜单测试：19 项通过；
- 数据库测试：5 项通过；
- 数据库迁移验证：通过；迁移 1300 首次执行成功，第二次正确跳过。

实施边界：

- 初始路由骨架早于 TC008 测试建立，因此没有观察到实施前的失败状态，本记录不宣称满足测试先行红灯证据。
- `BLOCKER-001` 至 `BLOCKER-003` 已由 D-164 与相应权威文档解除；类别增删改查和版本接口按已批准契约继续实现。

## 检查点二——权威契约、数据结构与权限

状态：通过

- D-164 已确定中文拼音元素、中文音节、汉字读音、两种语言句子—词语关系及类别权限。
- Content 表基线更新为 36 张；中文和老挝语结构关系仍使用独立物理表。
- 新增迁移 `1310_content_language_structures.sql`：一次性移除空旧中文拼音结构并建立六张新结构表；发现旧数据时直接停止。
- 新增迁移 `1320_content_language_permissions.sql`：登记 36 项类别权限，只默认授予 `super_admin`，并绑定侧边栏读取和审核可见性。
- 既有老挝语字母管理接口已切换到 `content.lo_letters.*` 精确权限。
- 数据库迁移首次执行与第二次幂等验证均通过；后端类型检查通过；权限与菜单单元测试共 21 项通过。

说明：迁移 1300 已在本地数据库执行，其字节内容必须冻结，因此保留原有两行英文注释；这属于迁移完整性要求，不是旧版运行兼容。

## 检查点 3：中老内容后端主链路

- 新增九类内容的字段契约、语言类别登记、通用版本聚合与状态变更校验。
- 新增数据库仓储，在发布事务内切换正式版本并物化到中文或老挝语专用结构表。
- 新增九条独立类别 API 路径，逐类别执行读写、审核、发布权限校验并记录成功操作审计。
- 真实 PostgreSQL 集成测试已覆盖中文五层和老挝语四层完整发布链路、重复组成项、未发布依赖与跨语言拒绝。
- `TC005` 红灯已确认后转绿；后端 Content 单元测试 16 项通过，Content 数据库集成测试 3 项通过，后端类型检查通过。

## 检查点 4：类别管理页面

- 九个类别页已接入独立 API、字段表单、搜索、状态筛选和有序组成编辑。
- 已接入草稿创建与编辑、提交审核、批准、驳回、退回编辑、正式发布和派生新版本。
- 页面操作按钮根据当前操作员的类别级权限启用；中文与老挝语依赖选择器只读取各自下级类别的已发布内容。
- 后台类型检查通过，路由和内容页面配置测试通过；版本比较、反向引用和集中审核视图继续在 T010 内完成。

## 检查点 5：版本影响分析、集中审核与完整验证

- 类别页新增不可变版本历史、任意两版逐字段比较和直接上级反向引用列表。
- 中文与老挝语审核页分别汇总本语言待审核和待发布版本，支持批准、驳回和发布预检。
- 新增中老语言内容管理 API 正式契约，并由 Content 域首页链接。
- 后台 119 项测试和生产构建通过；11 项 Chromium 端到端测试通过，其中包含 12 个内容页面和键盘可达性冒烟。
- 后端 189 项非集成测试、Content 真实 PostgreSQL 集成测试 3 项、数据库测试 5 项通过。
- 文档元数据、权威、生命周期和 Feature 目录审计通过，VitePress 文档构建通过。
- 后端完整 `verify` 的类型检查通过；其架构检查仍报告 4 条本功能范围外的既有跨域导入违规，位置均在 `admin-operator-provisioning` 与 Operations 组合代码，本功能未修改这些边界。

## 检查点 6：完整验证收敛

- 完整验证首轮识别 3 个严重缺口，并追加 T012、TC013、T014，不降低已批准规格。
- 审核发布页现可打开版本比较、使用 ConfirmDialog 确认发布，并在发布失败后以 `role=alert` 保留结构化阻塞信息。
- TC013 先确认 6 项浏览器测试失败，再完成实现使 6 项全部转绿；完整后台浏览器回归共 15 项通过。
- 12 个内容页面均执行主内容区域 WCAG-AA axe 检查；全局侧边栏既有对比度问题被排除在本功能选择器之外并未掩盖。
- `traceability.yml` 已补齐 US-001—US-005 到旅程、FR、任务、代码、测试和组件的实时映射；确定性校验 0 个错误。
- `@axe-core/playwright` 4.13.0 已完成注册表审计：存在 1681 天，最近一个月下载量 36,125,740，判定通过。

验证结果：后台 119 项测试通过，后台生产构建通过，完整 Chromium 端到端测试 15 项通过。
