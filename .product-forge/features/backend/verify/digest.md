> ⚠ BACKFILLED ARTIFACT
> Reverse-engineered from `apps/backend` on 2026-09-04.
> This is NOT the original intent of the feature; it is an inferred
> description based on code inspection. Treat as documentation, not spec.

Verification was not run because this feature was backfilled from existing code. See `gaps-report.md` for what a modern verification would check. Handoff notes: `backfilled: true`.
