> ⚠ BACKFILLED ARTIFACT
> Reverse-engineered from `apps/backend` on 2026-09-04.
> This is NOT the original intent of the feature; it is an inferred
> description based on code inspection. Treat as documentation, not spec.

The product summary was reconstructed from source organization, module names, HTTP registration, and tests. It contains no original-intent claim. Handoff notes: `backfilled: true`.
