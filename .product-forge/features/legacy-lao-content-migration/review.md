# 再验证结果：需要去重冲突裁决

> 状态：`READY_FOR_APPROVAL`  
> 阶段：revalidation

## 已验证的内容

- 目标 PostgreSQL 中三类 Lao Content 当前均为空。
- R2 映射与迁移操作员已配置。
- 目标 draft-only 生命周期、Asset/Audio 边界和源库 published-revision 读取范围与产品规格一致。

## 阻断性冲突

用户要求“去重、保留结构关系、保留音频”。实时源数据表明，同一规范 Lao 文本的重复记录并非同一内容的无害副本：

| 类型 | 重复组 | 中文释义不同组 | 描述不同组 | 组成关系不同组 | 音频不同组 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 音节 | 11 | 10 | 2 | 11 | 11 |
| 词语 | 3 | 3 | 3 | 3 | 3 |

去重后每个文本只能有一个目标 Content，因此无法无损保留每组中多套不同的组成关系与音频资产。目标模型的文本唯一约束也禁止保留多个同文本父实体。

## 已确认裁决

用户已确认：同一规范 Lao 文本以最小稳定源 ID 为 canonical，保留它的组成关系与音频；其他源版本完整写入隔离报告。

该规则解决了 F-001；未选中的来源记录及其结构/音频必须留在迁移隔离报告中，不能静默丢弃。

## 缺失关系例外

用户已确认：两条指向不存在词语的 sentence→word 关系不阻断句子导入。迁移必须跳过这两条关系，并在隔离报告中记录句子源 ID、缺失词语源 ID、位置和原因；同一语句的其他有效关系仍按顺序导入。
