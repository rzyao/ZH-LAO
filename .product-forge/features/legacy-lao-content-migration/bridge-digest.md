# Bridge — Digest

The approved migration scope is now expressed as the canonical Spec Kit specification at `specs/007-legacy-lao-content-migration/spec.md`.

It preserves the user-approved canonical-source algorithm, draft-only lifecycle, R2 asset mapping, valid-relation preservation and missing-relation isolation. It does not create a second source of truth in Product Forge; this directory contains only a pointer for lifecycle traceability.
