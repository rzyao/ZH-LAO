# 菜单树编辑器：设计系统清单

- **框架：** React；无 Storybook。
- **组件：** 复用现有 `Button`、`Dialog` 与 `ConfirmDialog`，没有新增组件库。
- **令牌：** 使用现有 `primary`、`card`、`muted`、`border` 变量及现有圆角等级。
- **本次页面：** 拖拽区域使用 `card` 表面、`border` 分隔和 `primary` 落点反馈；状态仍有文字与图标，不依赖颜色。
