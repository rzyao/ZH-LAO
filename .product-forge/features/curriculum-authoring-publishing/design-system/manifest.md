# Design System Manifest：课程编排与发布

> Read-only harvest from `apps/admin/src/components` and `apps/admin/src/design-system/tokens/index.css`.

React Admin 已提供 Button、DataTable、StatusBadge、ConfirmDialog、EditPageLayout 与 EmptyState。课程管理必须复用它们及 `--primary`、`--background`、`--foreground`、`--radius-*` token；本 manifest 不复制或修改设计系统。
