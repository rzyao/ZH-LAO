# Use a disposable PostgreSQL database. Never commit real credentials.
DATABASE_URL=postgresql://user:password@localhost:5432/zh_lao_v2_validation

# Optional server-level connection used only by validate.mjs to create a new
# uniquely named validation database. The database component should normally be
# "postgres".
ADMIN_DATABASE_URL=postgresql://user:password@localhost:5432/postgres
