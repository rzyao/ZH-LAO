# Expo / React Native tooling (Metro, NativeWind -> react-native-css-interop)
# resolves packages by walking up node_modules. pnpm's default isolated layout
# breaks that resolution, so this app uses the hoisted linker.
node-linker=hoisted
