# Admin 应用规范

## 设计系统

新增或修改 `apps/admin` 内的任何页面、组件或样式前，必须先完整阅读并严格遵循同目录下的 `DESIGN.md`。

`DESIGN.md` 是 admin 应用的唯一视觉与交互设计基准，涵盖设计令牌、排版、间距、组件状态、布局、响应式行为、可访问性及加载、空状态和错误状态。若现有实现与该规范冲突，新增工作应以 `DESIGN.md` 为准。
