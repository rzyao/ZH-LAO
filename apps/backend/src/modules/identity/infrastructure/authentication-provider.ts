import type { FastifyRequest } from 'fastify';import type { AuthenticationProvider } from '../../../auth/authentication-provider.js';import type { AuthContext } from '../../../auth/auth-context.js';import { AccessTokenService } from '../application/services/index.js';import type { IdentityRepositories } from '../application/ports/index.js';import type { DatabaseExecutor } from '../../../database/executor.js';
export class IdentityAuthenticationProvider implements AuthenticationProvider {constructor(private readonly tokens:AccessTokenService,private readonly repositories:(e:DatabaseExecutor)=>IdentityRepositories,private readonly db:DatabaseExecutor){}async authenticate(request:FastifyRequest):Promise<AuthContext|null>{const value=request.headers.authorization;if(!value?.startsWith('Bearer '))return null;try{const {subject}=this.tokens.verify(value.slice(7));const user=await this.repositories(this.db).users.findByPublicId(subject);return user?.status==='active'?{subjectId:user.publicId}:null;}catch{return null;}}}
