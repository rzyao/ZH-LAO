import { AppError } from '../../../../errors/app-error.js';import type{TransactionManager}from'../../../../database/transaction-manager.js';import type{IdentityAccountStatus,UserPublicId}from'../../domain/index.js';import type{IdentityRepositories}from'../ports/index.js';import type{IdentityEventWriter}from'../services/index.js';
export class IdentityState {constructor(private readonly tx:TransactionManager,private readonly repos:(e:import('../../../../database/executor.js').DatabaseExecutor)=>IdentityRepositories,private readonly events?:IdentityEventWriter,private readonly now:()=>Date=()=>new Date()){}async getCurrentIdentity(id:UserPublicId){const u=await this.tx.run(e=>this.repos(e).users.findByPublicId(id));if(!u)throw new AppError({code:'UNAUTHENTICATED',message:'Identity unavailable',httpStatus:401});return{userPublicId:u.publicId,status:u.status};}async changeStatus(id:UserPublicId,next:IdentityAccountStatus){return this.tx.run(async e=>{const r=this.repos(e);const current=await r.users.findByPublicId(id);if(!current)throw new AppError({code:'UNAUTHENTICATED',message:'Identity unavailable',httpStatus:401});await r.users.lockByInternalId(current.id);if(current.status===next||current.status==='closed'||!((current.status==='active'&&['disabled','closed'].includes(next))||(current.status==='disabled'&&['active','closed'].includes(next))))throw new AppError({code:'INVALID_DATA',message:'Invalid status transition',httpStatus:409});const updated=await r.users.updateStatus(current.id,next);const now=this.now();if(next!=='active')await r.sessions.revokeAllByUserId(current.id,now,'account_status_changed');if(this.events)await this.events.accountStatusChanged(e,{userPublicId:current.publicId,previous:current.status,next,occurredAt:now});return{userPublicId:updated!.publicId,status:updated!.status};});}}
