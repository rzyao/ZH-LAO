import crypto from 'node:crypto'; import pg from 'pg'; import pino from 'pino'; import { afterAll,beforeAll,describe,expect,it } from 'vitest';
import { createPgPool } from '../../src/database/pool.js'; import { TransactionManager } from '../../src/database/transaction-manager.js'; import { AudioPublishService } from '../../src/modules/audio/application/audio-publish-service.js'; import { createTestDatabase,type TestDatabase } from '../support/test-database.js';
const adminUrl=process.env.ADMIN_DATABASE_URL; const integration=adminUrl?describe:describe.skip;
integration('AudioPublishService PostgreSQL',()=>{let database:TestDatabase;let pool:pg.Pool;beforeAll(async()=>{database=await createTestDatabase(adminUrl!);pool=createPgPool({url:database.url,poolMin:0,poolMax:4,connectionTimeoutMs:2000,idleTimeoutMs:2000},pino({level:'silent'}));},120000);afterAll(async()=>{await pool?.end();await database?.dispose();});
 it('atomically moves an approved version to the official pointer and publishes its task',async()=>{const slot=crypto.randomUUID(),task=crypto.randomUUID(),attempt=crypto.randomUUID(),asset=crypto.randomUUID();await pool.query(`INSERT INTO audio.audio_slots(id,source_domain,content_entity_type,content_entity_id,language_code,audio_role,required_content_revision_id,required_audio_input_hash) VALUES($1,'content','lo_letter',$2,'lo','pronunciation',$3,'h')`,[slot,crypto.randomUUID(),crypto.randomUUID()]);await pool.query(`INSERT INTO audio.audio_tasks(id,slot_id,production_method,status,content_revision_id,text_snapshot,audio_input_hash,tts_preset_key,created_by_operator_id,client_idempotency_key) VALUES($1,$2,'tts','approved',$3,'x','h','p',$4,$5)`,[task,slot,crypto.randomUUID(),crypto.randomUUID(),crypto.randomUUID()]);await pool.query(`INSERT INTO audio.audio_generation_attempts(id,task_id,attempt_no,request_id,status) VALUES($1,$2,1,$3,'succeeded')`,[attempt,task,crypto.randomUUID()]);await pool.query(`INSERT INTO audio.audio_asset_versions(id,slot_id,task_id,version,generation_attempt_id,content_revision_id,audio_input_hash,asset_id,duration_ms,review_status) VALUES($1,$2,$3,1,$4,$5,'h',$6,100,'approved')`,[asset,slot,task,attempt,crypto.randomUUID(),crypto.randomUUID()]);const service=new AudioPublishService(new TransactionManager(pool,pino({level:'silent'})));const first=await service.publish(asset,'publish-1');expect(first.replayed).toBe(false);await expect(service.publish(asset,'publish-1')).resolves.toMatchObject({taskId:task,replayed:true});expect((await pool.query(`SELECT official_asset_version_id FROM audio.audio_slots WHERE id=$1`,[slot])).rows[0]).toMatchObject({official_asset_version_id:asset});expect((await pool.query(`SELECT status FROM audio.audio_tasks WHERE id=$1`,[task])).rows[0]).toMatchObject({status:'published'});expect((await pool.query(`SELECT first_published_at FROM audio.audio_asset_versions WHERE id=$1`,[asset])).rows[0]!.first_published_at).toBeTruthy();});});
