import { describe,expect,it } from 'vitest';
import { AccessTokenService,RefreshTokenService } from '../../src/modules/identity/application/services/index.js';
import { parseUserPublicId } from '../../src/modules/identity/domain/index.js';
describe('IDN-08 token services',()=>{const service=new AccessTokenService('test-jwt-secret-that-is-long-enough-for-hmac','issuer','audience');const user=parseUserPublicId('00000000-0000-4000-8000-000000000001');it('issues and verifies a minimal HS256 JWT',()=>{const token=service.issue(user);expect(service.verify(token).subject).toBe(user);expect(token).not.toContain('phone');expect(token).not.toContain('123');});it('uses distinct opaque random refresh tokens and deterministic hashes',()=>{const refresh=new RefreshTokenService();const a=refresh.prepare();const b=refresh.prepare();expect(a.rawRefreshToken).not.toBe(b.rawRefreshToken);expect(a.rawRefreshToken).not.toBe(a.hash);expect(refresh.hash(a.rawRefreshToken)).toBe(a.hash);});});
