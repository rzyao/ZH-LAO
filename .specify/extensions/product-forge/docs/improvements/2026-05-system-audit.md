# Product Forge — System Audit (2026-05-29)

> **Method:** 9-dimension multi-agent audit (cross-reference integrity, config-key consistency, status-schema, ID/naming, phase-map, count/version drift, contradictions, markdown structure, JS/shell scripts). Every candidate finding was adversarially re-verified against the cited bytes and classified; only genuine defects (`real=true`) are listed as actionable below. Defensible-imprecision and false-positive items are kept in the appendix for transparency.
> **Scope:** `commands/*.md`, `docs/**/*.md`, `scripts/*`, `extension.yml`, `config-template.yml`, `README.md`, `CHANGELOG.md`.

## Summary

| Severity | Count |
|----------|------:|
| CRITICAL | 0 |
| HIGH | 12 |
| MEDIUM | 29 |
| LOW | 15 |
| **Total actionable** | **56** |
| Reviewed, not actionable | 6 |

Highlights: the highest-impact defects are **broken markdown code-fence nesting in 6 command/template files** (inner ```` ``` ```` fences silently close the outer block, so an LLM rendering the template mis-parses it), a **`sync-verify` 7-layer↔9-layer self-contradiction**, **status-schema enum drift** (`completed_with_known_issues`, `rolled_back`, supporting-command phase keys not in the canonical schema), and **orphaned/undocumented config keys** (`a11y_gate` has no reader, `supply_chain.license_allowlist` / `supported_locales` / `constitution_path` read by commands but absent from the template/`config.md`).

## HIGH (12)

### A-01 — a11y_gate documented as a behavior switch, but no command reads it (axe is unconditional)
- **Dimension:** config-keys · **Location:** `docs/config.md:417, 434-457` · **Class:** genuine_contradiction (high confidence)
- **Problem:** config.md and config-template.yml define a11y_gate (axe|none, default axe) and state that none "suppresses generation of the automated floor" and that the Phase 8A test generator emits the axe check / Phase 8B runs it based on this key. But neither commands/test-plan.md nor commands/test-run.md reads a11y_gate anywhere — the @axe-core/playwright WCAG-AA test is generated unconditionally (test-plan.md §4) and executed unconditionally (test-run.md §4.7). Setting a11y_gate: none has no effect, so the docs mislead an LLM executing those phases.
- **Evidence:** config.md:451-452: "`none` suppresses generation of the automated floor."  config.md:417: "When `axe`, the Phase 8A generator emits one `@axe-core/playwright` check per `JRN` and Phase 8B runs it (test-run §4.7)."  test-plan.md:396-398: "**(v1.6, W5-B2) one WCAG-AA accessibility test per `JRN`** using `@axe-core/playwright` … then `expect(a11y.violations, 'JRN-NNN …').toEqual([])`" (emitted with no conditional read of a11y_gate). grep for "a11y_gate" across commands/ returns zero hits.
- **Fix:** Either (a) add an explicit step to test-plan.md §4 and test-run.md §4.7 that reads `a11y_gate` from `.product-forge/config.yml` and skips axe generation/execution when `none`; or (b) if the floor is intentionally always-on, correct config.md/config-template.yml to remove the false claim that `none` suppresses it.

### A-02 — Config key supply_chain.license_allowlist read by a command but absent from template and config.md
- **Dimension:** config-keys · **Location:** `commands/release-readiness.md:327-328` · **Class:** broken_reference (high confidence)
- **Problem:** release-readiness.md reads `.product-forge/config.yml` key `supply_chain.license_allowlist`, but this key exists in neither config-template.yml nor docs/config.md. A user copying the template has no way to know this key exists or how to set it; an LLM auditing config consistency would never surface it.
- **Evidence:** release-readiness.md:327-328: "Read the allowlist from `.product-forge/config.yml` key `supply_chain.license_allowlist` (literal default below if unset)."  grep for "supply_chain"/"license_allowlist" across the repo returns only commands/release-readiness.md.
- **Fix:** Add a `supply_chain:` block with `license_allowlist:` (default MIT,Apache-2.0,BSD-2-Clause,BSD-3-Clause,ISC,0BSD,Unlicense) to config-template.yml and document it in docs/config.md.

### A-03 — sync-verify described as both 7-layer and 9-layer (how-it-works contradicts itself, CHANGELOG, and improvements doc)
- **Dimension:** contradictions · **Location:** `docs/how-it-works-v2.md:347` · **Class:** genuine_contradiction (high confidence)
- **Problem:** The same sync-verify check is normatively stated as 'Seven-layer' in how-it-works-v2.md §12 (and '7-layer' in CHANGELOG.md and the improvements doc) but as '9-layer' in sync-verify.md, runtime.md, README.md, and even elsewhere within how-it-works-v2.md itself (§5). An LLM reading §12 would run only 7 layers and skip Layer 8 (contract-drift) and Layer 9 (doc↔code). The §12 artifact-pair diagram compounds the error: it chains only 6 artifacts (5 pairs) — research↔product-spec↔spec.md↔plan.md↔tasks.md↔code — with no contract or doc↔code leg, matching the stale 7-layer count.
- **Evidence:** SEVEN-LAYER side — docs/how-it-works-v2.md:347: "Seven-layer consistency check between artifact pairs:" and :350 diagram "research ↔ product-spec ↔ spec.md ↔ plan.md ↔ tasks.md ↔ code" (no contract/doc legs); CHANGELOG.md:287: "**`speckit.product-forge.sync-verify`** — Cross-cutting 7-layer artifact consistency checker:"; docs/improvements/2026-05-sdd-flow-improvements.md:21: "a 7-layer `sync-verify` drift checker". NINE-LAYER side — commands/sync-verify.md:29: "**Empty** → list features and ask which to check, then run full 9-layer scan"; docs/runtime.md:235: "9-layer check. The orchestrator also suggests this before Phase 7"; docs/how-it-works-v2.md:159 (same file, §5): "`/sync-verify` — 9-layer drift check (incl. contract-drift + doc↔code)".
- **Fix:** Update docs/how-it-works-v2.md §12 heading to 'Nine-layer', extend the artifact-pair diagram to include Layer 8 (FE↔BE contract drift) and Layer 9 (doc↔code), and fix CHANGELOG.md:287 and docs/improvements/2026-05-sdd-flow-improvements.md:21 to '9-layer'.

### A-04 — Canonical v3 status enum drops `completed_with_known_issues`, which runtime.md requires and forge.md actually writes
- **Dimension:** contradictions · **Location:** `docs/schema.md:106` · **Class:** genuine_contradiction (high confidence)
- **Problem:** The canonical schema (schema.md field catalog and the schema.yml preamble) defines the `phases.<name>.status` enum as exactly five values, omitting `completed_with_known_issues`. But runtime.md §4 — the pre-flight validator — lists `completed_with_known_issues` as a valid status, and forge.md Phase 8B literally writes it. Since runtime.md §4 mandates 'On any invalid value: abort ... Do NOT auto-correct', a validator that follows the *canonical* schema (the one schema.md and schema.yml call authoritative) would abort on a status value that forge.md emits during normal operation.
- **Evidence:** OMITTING (canonical) side — docs/schema.md:106: "`phases.<name>.status` | yes | one of `pending | in_progress | completed | skipped | not_applicable`. The `revalidation` phase additionally accepts `approved`..." and docs/schema/forge-status-v3.schema.yml:57-63 lists only "pending / in_progress / completed / skipped / not_applicable" (no completed_with_known_issues). USING side — docs/runtime.md:180-182: "`\"pending\" | \"in_progress\" | \"completed\" | \"skipped\" | \"not_applicable\" | \"completed_with_known_issues\"`, plus the legacy literal `\"approved\"`"; commands/forge.md:734: "Update `.forge-status.yml`: `test_run.status = completed` (or `completed_with_known_issues`)." (file-structure.md:205 also documents `completed_with_known_issues` as used by test_run/verify).
- **Fix:** Add `completed_with_known_issues` to the status enum in both docs/schema.md:106 and docs/schema/forge-status-v3.schema.yml (lines 56-63 valid-values comment), so the canonical schema matches runtime.md §4's validator list and forge.md's writes.

### A-05 — spec.md template fenced block is broken by unescaped inner ```typescript fences
- **Dimension:** formatting · **Location:** `commands/bridge.md:109-281` · **Class:** real_bug (high confidence)
- **Problem:** The spec.md template is wrapped in an outer ```markdown fence opened at line 109 and intended to run through line 376, but it contains unescaped inner ```typescript code fences (lines 275, 300). Under CommonMark, the bare closing fence at line 281 (closing the first typescript block) terminates the outer ```markdown wrapper prematurely. Everything from line 282 onward (Consumer Utilities, Fallback Behaviour, Acceptance Criteria, Success Metrics, Testing Specification, Risks, Wireframes, Open Questions) then renders as live document instead of template body, and the line-376 fence intended to close the template instead opens a new block, cascading the fence pairing for the rest of the file. An LLM reading this to reproduce spec.md cannot tell where the template ends. Sibling files api-docs.md, security-check.md, and test-plan.md correctly backslash-escape such inner fences.
- **Evidence:** L109: ```markdown ... L275: ```typescript ... L281: ``` ... L300: ```typescript ... L305: ```
- **Fix:** Backslash-escape the inner code fences inside the spec.md template (write \`\`\`typescript / \`\`\`) exactly as api-docs.md/security-check.md/test-plan.md do, or raise the outer wrapper to a 4-backtick fence (````markdown) so the inner 3-backtick fences nest correctly.

### A-06 — REV-NNN finding template broken by unescaped inner ```{language} fence
- **Dimension:** formatting · **Location:** `commands/code-review.md:195-217` · **Class:** real_bug (high confidence)
- **Problem:** The REV-NNN finding template is wrapped in an outer ```markdown fence at line 195. It contains an unescaped inner code fence ```{language} at line 210 whose bare closing fence at line 216 closes the outer wrapper prematurely under CommonMark; the line-217 fence then opens a new, unterminated-feeling block. The 'Suggested fix' Before/After code-block portion of the template (lines 210-216) is therefore mis-rendered and the template boundary is wrong.
- **Evidence:** L195: ```markdown L196: ### REV-{NNN}: {short title} ... L209: **Suggested fix:** L210: ```{language} L211: // Before ... L216: ``` L217: ```
- **Fix:** Escape the inner fences (\`\`\`{language} ... \`\`\`) inside the template, or use a 4-backtick outer wrapper (````markdown) so the 3-backtick inner block nests.

### A-07 — Wireframes template: ASCII-diagram ``` fence inside outer ```markdown block closes it early
- **Dimension:** formatting · **Location:** `commands/product-spec.md:373-399` · **Class:** real_bug (high confidence)
- **Problem:** In Step 4C the wireframes.md template is wrapped in ```markdown at line 373. It contains a bare inner code fence at line 383 (around the ASCII box diagram) which, under CommonMark, IS a valid closing fence and terminates the outer ```markdown wrapper at line 383. The ASCII diagram (lines 384-394) and the trailing template lines (Components/States and the intended outer close at 399) then render as live document with shifted fence pairing. The template body the author intended to be one ```markdown block is split.
- **Evidence:** L373: ```markdown ... L383: ``` L384: ┌─────────────────────────────────┐ ... L395: ``` ... L399: ```
- **Fix:** Use a 4-backtick outer wrapper (````markdown) so the inner 3-backtick ASCII-diagram fence nests, or backslash-escape the inner ``` fences at lines 383 and 395.

### A-08 — retrospective.md template: inner ``` Lifecycle/Funnel fences close the outer ```markdown block early
- **Dimension:** formatting · **Location:** `commands/retrospective.md:161-281` · **Class:** real_bug (high confidence)
- **Problem:** The retrospective.md report template is wrapped in ```markdown at line 161 and intended to run to line 281. It contains bare inner code fences for the 'Lifecycle Summary' block (lines 169, 181) and the 'Analytics Funnel' block (lines 209, 218). Under CommonMark the first bare inner fence at line 169 closes the outer ```markdown wrapper, so everything from line 170 to 281 renders as live document with broken fence pairing instead of as the template body.
- **Evidence:** L161: ```markdown ... L169: ``` L170: Phase 0 Problem Discovery  → {date} — {duration} ... L209: ``` ... L218: ```
- **Fix:** Raise the outer wrapper to a 4-backtick fence (````markdown) so the inner ``` Lifecycle/Funnel blocks nest, or backslash-escape the inner fences at lines 169/181/209/218.

### A-09 — tracking-plan.md template: inner ``` Funnel fences close the outer ```markdown block early
- **Dimension:** formatting · **Location:** `commands/tracking-plan.md:114-248` · **Class:** real_bug (high confidence)
- **Problem:** The tracking-plan.md report template is wrapped in ```markdown at line 114 and intended to run to line 248. It contains bare inner code fences for the two funnel diagrams (lines 216, 223 and 227, 232). Under CommonMark the bare inner fence at line 216 closes the outer ```markdown wrapper, so the Funnels/Coverage Matrix/Predicted Metrics sections (lines 217-248) render as live document with shifted fence pairing rather than as template body.
- **Evidence:** L114: ```markdown ... L216: ``` L217: {feature_slug}_viewed ... L223: ``` ... L227: ``` ... L232: ```
- **Fix:** Raise the outer wrapper to a 4-backtick fence (````markdown) so the inner ``` funnel blocks nest, or backslash-escape the inner fences at lines 216/223/227/232.

### A-10 — Portfolio report template: inner ```mermaid fence closes the outer ```markdown block early and leaves an unterminated fence at EOF
- **Dimension:** formatting · **Location:** `docs/templates/portfolio-report.md:9-103` · **Class:** real_bug (high confidence)
- **Problem:** The portfolio.md report template is wrapped in ```markdown at line 9 and intended to close at line 103. It contains an inner ```mermaid block (lines 42-48). The mermaid block's bare closing fence at line 48 closes the outer ```markdown wrapper under CommonMark, so sections 4-7 (lines 49-102) render as live document, and the bare ``` at line 103 (the author's intended template close) instead OPENS a new code block that is never closed — leaving the file with a literally unterminated code fence at EOF. This is the only file in the tree that ends with an open fence.
- **Evidence:** L9: ```markdown ... L42: ```mermaid ... L48: ``` ... L103: ``` (UNCLOSED to EOF)
- **Fix:** Raise the outer wrapper to a 4-backtick fence (````markdown) so the inner ```mermaid block nests and line 103 closes the outer block correctly; alternatively backslash-escape the inner ```mermaid / ``` fences.

### A-11 — Gate Entry Schema in file-structure.md is missing the `rolled_back` decision value (5-value vs canonical 6-value enum)
- **Dimension:** schema · **Location:** `docs/file-structure.md:215` · **Class:** genuine_contradiction (high confidence)
- **Problem:** The Gate Entry Schema lists only 5 decision literals (`approved | approved_with_conditions | revised | skipped | aborted`), omitting `rolled_back`, which is present in the canonical schema (gates[].decision, line 149), policy.md §2 (line 55), and runtime.md (line 247). It also omits the `rolled_back_to`, `reviewed_sha`, and `risk` gate fields the canonical schema defines. An LLM following this block would not record rollbacks correctly.
- **Evidence:**   decision: "approved"                # approved | approved_with_conditions | revised | skipped | aborted
- **Fix:** Update the decision comment to the canonical 6-value enum `approved | approved_with_conditions | revised | skipped | rolled_back | aborted` and add the `rolled_back_to`, `reviewed_sha`, and `risk` fields (or point to the canonical schema).

### A-12 — Phase-status enum in runtime.md includes `completed_with_known_issues`, which the canonical schema and schema.md do NOT list — two normative validators disagree
- **Dimension:** schema · **Location:** `docs/runtime.md:179-193` · **Class:** genuine_contradiction (high confidence)
- **Problem:** runtime.md §4 step 2 mandates that every `phases.<name>.status` MUST be one of `pending | in_progress | completed | skipped | not_applicable | completed_with_known_issues` and that on any out-of-enum value the orchestrator MUST abort and "Do NOT auto-correct". But the canonical schema (forge-status-v3.schema.yml:56-65) and schema.md:106 only define `pending | in_progress | completed | skipped | not_applicable` (plus `approved` on revalidation) — they do NOT include `completed_with_known_issues`. Meanwhile test-run.md:890 and forge.md:734 legitimately write `completed_with_known_issues`. So a strict reader following the canonical schema would abort on a status that another command is instructed to write, while a reader following runtime.md accepts a value the canonical schema deems invalid. (file-structure.md:205 additionally attributes the value to `verify`, but verify-full is read-only per forge.md:685 and writes no phase status.)
- **Evidence:** Every `phases.<name>.status` MUST be one of      `"pending" | "in_progress" | "completed" | "skipped" |      "not_applicable" | "completed_with_known_issues"`
- **Fix:** Pick one canonical definition. Either add `completed_with_known_issues` to the phases status enum in forge-status-v3.schema.yml (lines 56-65) and schema.md:106, or remove it from runtime.md:181 / test-run.md:890 / forge.md:734 / file-structure.md and have those phases write `completed` with the known-issue captured elsewhere.

## MEDIUM (29)

### A-13 — License-allowlist env override uses PF_ prefix, contradicting the documented PRODUCT_FORGE_ prefix
- **Dimension:** config-keys · **Location:** `commands/release-readiness.md:332` · **Class:** drift (high confidence)
- **Problem:** config.md states every config value is overridable via the `PRODUCT_FORGE_` environment-variable prefix. release-readiness.md instead reads the override from `PF_LICENSE_ALLOWLIST`. Per the documented convention the override for supply_chain.license_allowlist would be `PRODUCT_FORGE_SUPPLY_CHAIN_LICENSE_ALLOWLIST`, so the command's env name is inconsistent with the documented scheme and would not be discovered by a user following config.md.
- **Evidence:** config.md:292: "Any config value can be overridden via environment variable using the prefix `PRODUCT_FORGE_`:"  release-readiness.md:332: "ALLOW=\"${PF_LICENSE_ALLOWLIST:-MIT,Apache-2.0,BSD-2-Clause,BSD-3-Clause,ISC,0BSD,Unlicense}\""
- **Fix:** Rename the env var in release-readiness.md to match the documented prefix (e.g. PRODUCT_FORGE_SUPPLY_CHAIN_LICENSE_ALLOWLIST), or document PF_LICENSE_ALLOWLIST as an explicit exception in config.md.

### A-14 — default_track_hint is defined in template + config.md but read by no command or doc (orphaned key)
- **Dimension:** config-keys · **Location:** `config-template.yml:146` · **Class:** drift (high confidence)
- **Problem:** config-template.yml and config.md define default_track_hint as "the default track the intake/triage step offers when nothing else is specified." But the intake/triage step in forge.md (Intake/Triage section) and policy.md §4.0 compute the recommended track purely from change classification and never read default_track_hint. The key has no consumer, so setting it has no effect.
- **Evidence:** config-template.yml:146: "default_track_hint: \"standard\""  config.md:415: "`default_track_hint` … Default track the intake/triage step offers when nothing else is specified."  grep for "default_track_hint" across commands/ and docs/ (excluding config-template.yml and config.md) returns zero hits; policy.md:121-138 §4.0 derives the recommendation from a classification table, not from config.
- **Fix:** Either wire default_track_hint into forge.md's intake step and policy.md §4.0 (use it as the pre-selected default in the Track prompt), or remove the key from config-template.yml and config.md.

### A-15 — constitution_path is a real config key (template + plan.md) but undocumented in config.md
- **Dimension:** config-keys · **Location:** `docs/config.md` · **Class:** drift (high confidence)
- **Problem:** constitution_path is present in config-template.yml (commented) and is actively read by plan.md to locate the architecture constitution, but it has no entry anywhere in docs/config.md. A user relying on config.md as the configuration reference will not learn the key exists.
- **Evidence:** config-template.yml:76: "# constitution_path: \".specify/memory/constitution.md\""  plan.md:119: "Read `.product-forge/config.yml` → look for `constitution_path` key"  grep for "constitution_path"/"constitution" in docs/config.md returns zero hits.
- **Fix:** Add a constitution_path entry to docs/config.md (under SpecKit Integration), documenting the default fallback to .specify/memory/constitution.md.

### A-16 — supported_locales read by i18n-harvest but absent from config-template.yml and config.md
- **Dimension:** config-keys · **Location:** `commands/i18n-harvest.md:33, 42` · **Class:** drift (high confidence)
- **Problem:** i18n-harvest.md reads `supported_locales` from `.product-forge/config.yml` to determine the locale set, but this key is defined in neither config-template.yml nor docs/config.md. A user enabling the i18n phase has no documented config key to set the locale list.
- **Evidence:** i18n-harvest.md:33: "Default: read `supported_locales` from project config or from the existing i18n folder structure."  i18n-harvest.md:42: "`supported_locales` in `.product-forge/config.yml`."  grep for "supported_locales" across config-template.yml and docs/config.md returns zero hits.
- **Fix:** Add supported_locales (list, default []) to config-template.yml and document it in docs/config.md, or note in i18n-harvest.md that it is intentionally an undocumented/auto-detected-only signal.

### A-17 — runtime.md §1 Step-0 Extract list is stale: omits flow_mode, which the orchestrator itself reads
- **Dimension:** config-keys · **Location:** `docs/runtime.md:19-33` · **Class:** drift (high confidence)
- **Problem:** runtime.md §1 enumerates the keys the orchestrator extracts at Step 0 and includes peers like default_feature_mode and release_readiness, but omits flow_mode. flow_mode is a top-level orchestration control that forge.md (the orchestrator) reads directly to choose gated vs fluid sequencing, so it belongs in the Step-0 extract list. The omission makes the normative extract list incomplete/stale for v1.6.
- **Evidence:** runtime.md:19-33 lists project_name … release_readiness with no flow_mode entry.  forge.md:159: "Read `flow_mode` from config (default `gated`):"  flow_mode appears in runtime.md only at line 490 (Fluid-Mode section), not in the §1 extract.
- **Fix:** Add `flow_mode` (gated | fluid, default gated) to the runtime.md §1 Extract list; while there, audit the list for other orchestrator-level v1.6 keys.

### A-18 — `feature_mode` enum stated as 3 values (lite/standard/v-model) in migration & config docs but 4 values (incl. express) in schema/runtime/forge
- **Dimension:** contradictions · **Location:** `docs/schema/migration-v2-to-v3.md:36` · **Class:** genuine_contradiction (high confidence)
- **Problem:** The valid `feature_mode` values are given as four (`express | lite | standard | v-model`) in schema.yml, schema.md, runtime.md §4, and forge.md's Mode Resolution validator — but as three (`lite/standard/v-model`, no `express`) in migration-v2-to-v3.md, config.md, and config-template.yml. An LLM relying on the migration or config docs for the enum would treat a legitimate `feature_mode: express` (a first-class v1.6 mode) as invalid, and forge.md §4 would abort it.
- **Evidence:** 4-VALUE side — docs/schema/forge-status-v3.schema.yml:28: "Valid values: \"express\" | \"lite\" | \"standard\" | \"v-model\"."; docs/schema.md:102: "`\"express\" | \"lite\" | \"standard\" | \"v-model\"`"; commands/forge.md:279: "The resolved value MUST be one of `\"express\" | \"lite\" | \"standard\" | \"v-model\"`". 3-VALUE side — docs/schema/migration-v2-to-v3.md:36: "`feature_mode` | Selects phase map (lite/standard/v-model). See E1. | `\"standard\"`"; docs/config.md:310-313 lists only "lite" / "standard" / "v-model" under default_feature_mode valid values; config-template.yml:119-131 same three-value Options block.
- **Fix:** Add `express` to the feature_mode value lists in docs/schema/migration-v2-to-v3.md:36, docs/config.md (default_feature_mode appendix lines 310-323), and config-template.yml (default_feature_mode Options, lines 119-131).

### A-19 — Standard mode described as '14-phase' in policy/config but '18-phase' in how-it-works and the QA test plan
- **Dimension:** contradictions · **Location:** `docs/policy.md:180` · **Class:** genuine_contradiction (high confidence)
- **Problem:** The standard-mode lifecycle length is stated as 14 phases in policy.md, config.md, and config-template.yml, but as 18 phases in how-it-works-v2.md §5 and the plugin QA test plan (which asserts forge.md must 'show 18 phases'). how-it-works-v2.md is internally inconsistent too: its mode table (§4) says standard is '14 (+ 4 opt)' while §5 calls it the 'Full 18-phase flow'. This misleads anyone counting phases to validate the phase map and produces a self-failing QA check.
- **Evidence:** 14-PHASE side — docs/policy.md:180: "**Standard mode** — the full 14-phase lifecycle documented in `commands/forge.md`."; docs/config.md:313: "`\"standard\"` — full 14-phase lifecycle. Default."; config-template.yml:122: "#   \"standard\" — full 14-phase lifecycle (recommended for most features)". 18-PHASE side — docs/how-it-works-v2.md:133: "Full 18-phase flow. Opt = optional; Cond = conditional"; docs/qa/plugin-test-plan.md:244: "all three show 18 phases" (and :56 "the full 14/18 phases"). Internal conflict — how-it-works-v2.md:104 mode table: "`standard` | 14 (+ 4 opt)".
- **Fix:** Pick one canonical count (the phase execution map in forge.md is the source of truth) and reconcile policy.md:180, config.md:313, config-template.yml:122, how-it-works-v2.md:104/133, and the QA test plan to the same number with a consistent opt/conditional accounting.

### A-20 — QA plan states 29 slash commands; repo has 31
- **Dimension:** counts-versions · **Location:** `docs/qa/plugin-test-plan.md:77` · **Class:** drift (high confidence)
- **Problem:** The QA test plan asserts there are 29 slash commands, but commands/ contains 31 .md files and extension.yml registers exactly 31 commands (verified: `ls commands/*.md | wc -l` = 31; extension.yml unique `speckit.product-forge.*` ids = 31; diff between the two sets is empty). An LLM running the QA plan's set-equality checks against an expected count of 29 would flag a false failure. (Note: the audit-task seed's hypothesis of 33 is also wrong; the true count is 31.)
- **Evidence:** - **29 slash commands** in `commands/*.md` covering problem-discovery,
- **Fix:** Change 29 to 31 here and audit-stamp the count, or replace the hard number with the dynamic `ls commands/*.md | wc -l` check the same doc already prescribes at line 243.

### A-21 — how-it-works-v2 says extension.yml 'registers all 29 commands' — actually 31
- **Dimension:** counts-versions · **Location:** `docs/how-it-works-v2.md:62` · **Class:** drift (high confidence)
- **Problem:** This line is factually false: extension.yml registers 31 commands, not 29 (verified by counting unique `speckit.product-forge.*` ids in extension.yml = 31). An LLM reading this normative architecture doc to understand wiring would be misled about how many commands exist.
- **Evidence:** ├── extension.yml                # registers all 29 commands + config keys + tags
- **Fix:** Update '29' to '31' (commands/ and extension.yml currently both contain 31; the two v1.6 commands design-system-harvest and spec-merge were added but the count was never updated).

### A-22 — README command table omits 2 registered commands (design-system-harvest, spec-merge)
- **Dimension:** counts-versions · **Location:** `README.md:56-83` · **Class:** broken_reference (high confidence)
- **Problem:** The README command table lists only 29 command rows (verified: 29 `| `/speckit.product-forge.` rows). The commands design-system-harvest and spec-merge are registered in extension.yml and exist as files but appear nowhere in README — neither in the table nor in prose (verified by comm of file set vs all README mentions). A user/LLM consulting README as the command catalog would not know these two commands exist.
- **Evidence:** NO v1.6 MENTION IN README  (and `comm -23` of command files vs README mentions returns: /speckit.product-forge.design-system-harvest, /speckit.product-forge.spec-merge)
- **Fix:** Add table rows for `/speckit.product-forge.design-system-harvest` and `/speckit.product-forge.spec-merge` so the README catalog matches the 31 registered commands.

### A-23 — Self-contradictory and stale forge.md LOC claim (560→640, real value 898)
- **Dimension:** counts-versions · **Location:** `docs/how-it-works-v2.md:68-69` · **Class:** genuine_contradiction (high confidence)
- **Problem:** The text says forge.md 'shrank from ~560 to ~640 LOC' — going from 560 to 640 is an INCREASE, not a shrink, so the sentence is internally self-contradictory. It is also stale: forge.md is actually 898 lines (verified `wc -l commands/forge.md` = 898; `awk END NR` = 898). Both the verb and both numbers are wrong.
- **Evidence:** Most commands delegate work to docs. `commands/forge.md` shrank from ~560 to ~640 LOC (more phases, less inline policy) by moving policy, runtime logic,
- **Fix:** Fix the contradiction and the number: either state it 'grew to ~900 LOC' or, if the intent was net reduction of inline policy despite added phases, rephrase to '~898 LOC after extracting policy/runtime into docs' — but do not claim a shrink while citing a larger number.

### A-24 — Manifest version is 1.6.0 but its own description says 'v1.5 adds…' and never mentions v1.6
- **Dimension:** counts-versions · **Location:** `extension.yml:6-7` · **Class:** genuine_contradiction (high confidence)
- **Problem:** The manifest declares version 1.6.0 (line 6) while its description (line 7) only advertises 'v1.5 adds portfolio view, lite mode, …' with no mention of any v1.6 feature set. The manifest contradicts itself on which release it represents — a reader/LLM parsing the manifest gets a 1.6.0 version stamp paired with a 1.5 feature narrative.
- **Evidence:**   version: "1.6.0"   description: "Full product lifecycle orchestrator with research, spec, plan, implement, verify, test, and release-readiness phases. v1.5 adds portfolio view, lite mode, brown-field backfill, monorepo support, V-Model optional integration, unit + integration test phases, monitoring/migration/i18n/experiment extensions, per-phase digests, and a learning loop. See README for the complete feature list."
- **Fix:** Update the description to reflect v1.6.0 (add the v1.6 capabilities: express mode, design-system-harvest, spec-merge, new verify layers, a11y gate, FE↔BE contract drift), or at minimum change 'v1.5 adds' to match the declared 1.6.0 version.

### A-25 — CHANGELOG has no 1.6.0 (or Unreleased) entry though shipped version is 1.6.0
- **Dimension:** counts-versions · **Location:** `CHANGELOG.md:9` · **Class:** drift (high confidence)
- **Problem:** extension.yml ships version 1.6.0, but the CHANGELOG's newest entry is [1.5.1] — 2026-04-24, with no 1.6.0 entry and no [Unreleased] section (verified: only [1.5.1] and the v1.5.0..v1.5.1 compare link exist for the top range). The entire v1.6 feature wave is undocumented in the changelog, contradicting README:24 which directs readers to 'Full change list in CHANGELOG.md'.
- **Evidence:** ## [1.5.1] — 2026-04-24
- **Fix:** Add a [1.6.0] CHANGELOG entry documenting the v1.6 wave (or an [Unreleased] section) and the corresponding compare link, so it matches extension.yml version 1.6.0.

### A-26 — Malformed table: unescaped pipes inside {…} create 4 cells against a 3-column header
- **Dimension:** formatting · **Location:** `commands/experiment-design.md:153-154` · **Class:** real_bug (high confidence)
- **Problem:** The 'Report' table in Step 3 has a 3-column header (| Parameter | Value | Source |, line 151-152). Rows 153 and 154 contain unescaped pipe characters inside the Value cell's braces ({proportion | continuous} and {two-proportion z-test | Welch t-test}). Because these pipes are NOT inside an inline-code span, GFM treats them as column delimiters, so each row parses as 4 cells. The table misaligns: the Source column shows 'continuous}' / 'Welch t-test}' and the real source ('hypothesis') overflows into a spurious 4th column, misrepresenting which value belongs to which column.
- **Evidence:** L151: | Parameter | Value | Source | L153: | Metric type | {proportion | continuous} | hypothesis | L154: | Formula | {two-proportion z-test | Welch t-test} | §3.{a,b} |
- **Fix:** Escape the literal pipes as \| (e.g. {proportion \| continuous}) or wrap the value in an inline-code span (`{proportion | continuous}`), which GFM treats as literal — matching the correct pattern used in docs/schema.md and the wave5 roadmap tables.

### A-27 — Test-case ID registry omits TC-UNIT and TC-INT families that commands treat as canonical
- **Dimension:** naming-ids · **Location:** `docs/file-structure.md:386-389` · **Class:** drift (high confidence)
- **Problem:** The naming-convention registry lists only four TC families (TC-SMK/TC-E2E/TC-API/TC-REG), but TC-UNIT-NNN and TC-INT-NNN are first-class, normatively-defined test-case ID families used throughout the plugin: defined in test-plan.md sections 5E/5F, consumed by test-run.md, listed in testing-strategy.md and the traceability-matrix.md template, and even referenced in the canonical v3 schema (red_gate.tests: ["TC-UNIT-021", ...]). An LLM consulting the registry as the authoritative ID list would not know TC-UNIT/TC-INT are valid, and the registry contradicts the very files that emit them.
- **Evidence:** file-structure.md:386-389: "| Smoke test case IDs | `TC-SMK-NNN` | ... | E2E test case IDs | `TC-E2E-NNN` ... | API test case IDs | `TC-API-NNN` ... | Regression test IDs | `TC-REG-NNN`" (no TC-UNIT / TC-INT rows). Contrast test-plan.md:274 "### 5E: Unit Test Cases (TC-UNIT-NNN)" and test-plan.md:327 "### 5F: Integration Test Cases (TC-INT-NNN)", plus forge-status-v3.schema.yml:101 "tests: [\"TC-UNIT-021\", \"TC-E2E-003\"]".
- **Fix:** Add two rows to the naming-convention table: `| Unit test case IDs | TC-UNIT-NNN | TC-UNIT-001 |` and `| Integration test case IDs | TC-INT-NNN | TC-INT-004 |`. Also update the directory-layout comment at file-structure.md:68 ("TC-SMK/E2E/API/REG-NNN") to include UNIT and INT.

### A-28 — Task-ID registry convention (T-NNN) matches no actual emitter; two real forms plus a third documented form are in use
- **Dimension:** naming-ids · **Location:** `docs/file-structure.md:397` · **Class:** genuine_contradiction (high confidence)
- **Problem:** The registry defines Task IDs as `T-NNN` (hyphenated, e.g. `T-001`, `T-042`), but no command or schema actually emits that form. tasks.md (the command that defines the task format) and forge-status-v3.schema.yml both use the unhyphenated `T001` form, while the traceability-matrix.md template and verify-full.md's example matrix use a third form, `TASK-012`/`TASK-013`. So the registry documents a form (`T-NNN`) used by nobody, and two distinct real forms (`T001` and `TASK-012`) coexist for the same concept. validate-traceability.js does reconcile `TASK-`->`T` and strips hyphens (script line 180), so the common zero-padded case does not break at runtime — but the conflicting conventions will mislead authors and any naive cross-reference.
- **Evidence:** file-structure.md:397 "| Task IDs | `T-NNN` (3 digits) | `T-001`, `T-042` |". tasks.md:219 "- [ ] T007 — Add push token storage to User schema" (no hyphen). forge-status-v3.schema.yml:136 "- id: \"T001\"                     # matches task id in tasks.md". traceability-matrix.md:70 "tasks: [\"TASK-012\", \"TASK-013\"]" and verify-full.md:577 "| ... | TASK-012, TASK-013 | ...".
- **Fix:** Pick one canonical task-ID form and align all four sources. Given tasks.md and the schema already agree on `T001`, change the registry row to `| Task IDs | T0NN (zero-padded, no hyphen) | T001, T042 |` and change traceability-matrix.md:70 / verify-full.md:577 from `TASK-NNN` to `T0NN` (or document `TASK-NNN` as an accepted alias in the registry).

### A-29 — REQ- (root of the traceability chain) is used as a starred ID family but has no registry row
- **Dimension:** naming-ids · **Location:** `docs/file-structure.md:375-398` · **Class:** drift (high confidence)
- **Problem:** REQ-NNN is the canonical living-spec requirement ID and the root of the entire traceability chain (REQ->US->JRN->...->TEST->EVT), used as `req:` in traceability.yml rows and read by validate-traceability.js (row.req), but the naming-convention table has no REQ- row. REQ- is distinct from the registered FR- (per-feature functional requirement), so it needs its own entry; an author or LLM consulting the registry has no definition for the chain's root ID.
- **Evidence:** No `REQ-` row exists in file-structure.md:375-398. Yet file-structure.md:29 "traceability.yml ← [v1.6] live matrix (REQ→US→JRN→…→TEST→EVT)", file-structure.md:463 "stable REQ-* requirements; merged by spec-merge", and file-structure.md:468 "(REQ→US→JRN→FR→CMP→API→TASK→code→TEST→EVT)"; traceability-matrix.md:63 "- req: \"REQ-001\""; validate-traceability.js:111 "const id = row.req || row.story ...".
- **Fix:** Add a registry row: `| Canonical requirement IDs | REQ-NNN | REQ-001 |`, and note its relationship to FR-NNN (REQ-* = stable living-spec requirement in specs/<domain>/spec.md; FR-* = per-feature functional requirement).

### A-30 — Schema docs bless only design_system_harvest and spec_merge as extra phase keys, but file-structure.md and three commands write api_docs/security_check/tracking_plan
- **Dimension:** naming-ids · **Location:** `docs/schema.md:102` · **Class:** genuine_contradiction (medium confidence)
- **Problem:** schema.md line 102 explicitly enumerates the additional valid `phases.<name>` helper keys as exactly `design_system_harvest` and `spec_merge`, and forge-status-v3.schema.yml's phases block lists every phase key but omits api_docs/security_check/tracking_plan. Yet file-structure.md:207-209 documents — and the api-docs, security-check, and tracking-plan commands actually emit — `phases.api_docs`, `phases.security_check`, and `phases.tracking_plan`. Two normative docs disagree about which phase keys are valid; an LLM validating against the canonical schema would not recognize the keys the supporting commands write.
- **Evidence:** schema.md:102 "Phase keys `design_system_harvest` (v1.6, UI features) and `spec_merge` (v1.6, living-spec merge) are also valid `phases.<name>` entries." (no mention of api_docs/security_check/tracking_plan). file-structure.md:207-209 "Supporting commands (`api-docs`, `security-check`, `tracking-plan`) also write completion status to `phases:` using their own keys (`api_docs`, `security_check`, `tracking_plan`)." Confirmed in commands: api-docs.md:485-486 "phases:\n  api_docs: completed"; security-check.md:313-314 "phases:\n  security_check: completed"; tracking-plan.md:362-363 "phases:\n  tracking_plan: completed".
- **Fix:** Reconcile the two normative docs: add api_docs/security_check/tracking_plan to the canonical schema (forge-status-v3.schema.yml phases block and schema.md:102 list) as recognized supporting-command phase keys, OR move the file-structure.md:207-209 note into schema.md so the canonical schema is the single source of the phase-key set.

### A-31 — config.md lists express-excluding "Valid values" for default_feature_mode, contradicting forge.md's validation set
- **Dimension:** phase-map · **Location:** `docs/config.md:310-323` · **Class:** genuine_contradiction (high confidence)
- **Problem:** docs/config.md presents an explicit "Valid values:" enumeration for default_feature_mode that lists only lite/standard/v-model and omits express. But commands/forge.md Mode Resolution (L278-281) validates the resolved value MUST be one of "express" | "lite" | "standard" | "v-model" and aborts otherwise. An LLM reading config.md as the config reference would treat express as an invalid feature_mode, directly contradicting the orchestrator's own validation and the fact that express is persisted to feature_mode (forge.md L141-143).
- **Evidence:** config.md L310-318: "Selects the phase map for new features. Valid values:\n- `\"lite\"` — 5-phase lifecycle ...\n- `\"standard\"` — full 14-phase lifecycle. Default.\n- `\"v-model\"` — Product Forge keeps the bookends ..." (no `express` entry). vs forge.md L278-281: "The resolved value MUST be one of `\"express\" | \"lite\" | \"standard\" | \"v-model\"`."
- **Fix:** Add an `- "express" — 4-phase combined pass (product-spec minimal → plan inline → implement → verify); first-class feature_mode, escalatable to lite/standard` entry to the default_feature_mode "Valid values" list in config.md so it matches forge.md's validated mode set.

### A-32 — phases.md Appendix C "Feature modes" table omits express mode
- **Dimension:** phase-map · **Location:** `docs/phases.md:453-457` · **Class:** drift (high confidence)
- **Problem:** phases.md Appendix C is a normative phase-map reference ("## Appendix C — Feature modes") that enumerates feature modes and their active/excluded phases, but lists only lite/standard/v-model. express is never mentioned anywhere in phases.md. Express is a first-class feature_mode (forge.md L278-281, policy.md §4.1 has an Express track phase map). An LLM consulting phases.md to resolve or validate a mode would not find express and could reject or mis-map an express feature.
- **Evidence:** phases.md L453-457: "| Mode | Active phases | Excluded phases |\n| `lite` | ... |\n| `standard` | all phases per the matrix above | — |\n| `v-model` | standard + V-Model artifact phases via `speckit:v-model-*` | — |" — no `express` row; grep for "express" in phases.md returns nothing.
- **Fix:** Add an `express` row to phases.md Appendix C with its active phases (product-spec minimal, plan inline, implement, verify) and "everything else → not_applicable", mirroring policy.md §4.1 and forge.md's execution map express column.

### A-33 — forge.md frontmatter description omits express from the feature_mode list
- **Dimension:** phase-map · **Location:** `commands/forge.md:5-6` · **Class:** genuine_contradiction (high confidence)
- **Problem:** The forge.md frontmatter description states the orchestrator drives the phase map selected by feature_mode "(lite / standard / v-model)", omitting express. The body of the same file treats express as first-class: it validates express in Mode Resolution (L278-281), has a dedicated "Express mode" section (L149-155), an express column in the execution map (L317-336), and an express traceability chain at completion (L891). The frontmatter is the first thing read and undercounts the supported modes.
- **Evidence:** forge.md L5-6: "shipped, measured code through the phase map selected by `feature_mode`\n  (lite / standard / v-model) with human-in-the-loop gates" vs L280-281: "The resolved value MUST be one of\n   `\"express\" | \"lite\" | \"standard\" | \"v-model\"`."
- **Fix:** Change the frontmatter mode list to "(express / lite / standard / v-model)" to match the body's validated mode set.

### A-34 — forge.md "Phase execution map by mode" omits 2H and Phase 10, disagreeing with forge.md's own "Phase Map (standard mode)" table
- **Dimension:** phase-map · **Location:** `commands/forge.md:317-349` · **Class:** genuine_contradiction (high confidence)
- **Problem:** forge.md contains two phase tables that disagree on which phases exist. The "Phase Map (standard mode)" table (L40-59) has 20 rows including "2H. Design System Harvest" and "10. Spec Merge". The "Phase execution map by mode" table (L317-349) has only 18 non-V rows and omits both 2H (Design System Harvest) and Phase 10 (Spec Merge) entirely — so the per-mode map gives no express/lite/standard/v-model column for two phases the same file declares part of the standard map. An LLM using the execution map to decide whether to run design-system-harvest or spec-merge in a given mode would find no row for them.
- **Evidence:** forge.md L43: "| 2H. Design System Harvest *(UI features; helper within Phase 2)* | ... |" and L59: "| 10. Spec Merge *(living spec; after release-readiness)* | ... |" both appear in the L40-59 Phase Map table; the L317-349 "Phase execution map by mode" jumps from "2. Product Spec" to "3. Revalidation" (no 2H row) and from "9B. Experiment Design" to the V-rows (no Phase 10 row).
- **Fix:** Add 2H (Design System Harvest) and 10 (Spec Merge) rows to the "Phase execution map by mode" table with their per-mode applicability, so both forge.md tables enumerate the same phase set.

### A-35 — file-structure.md presents a stale v2 status block as the schema, with fields/values absent from canonical v3
- **Dimension:** schema · **Location:** `docs/file-structure.md:120-209` · **Class:** drift (high confidence)
- **Problem:** The section is titled "## .forge-status.yml Schema (v2)" and is the only inline status schema in file-structure.md, yet v3 is canonical (forge-status-v3.schema.yml). It documents top-level extension blocks (problem, research_dimensions, input_richness_score, implement, api_docs, security, tracking, retrospective) that do NOT exist in the canonical v3 schema; lists phase status value `completed_with_known_issues` (line 205) which canonical omits; and its phase-status comment (line 128) lists `completed_with_known_issues` but omits the canonical value `not_applicable`. An LLM reading this as the schema reference will write and validate against fields/values the canonical schema rejects.
- **Evidence:** ## .forge-status.yml Schema (v2)  ...  research: pending  # pending | in_progress | completed | skipped | completed_with_known_issues  ...  implement:  # populated by implement (Phase 6)  ...  api_docs:  # populated by api-docs command  ...  | `completed_with_known_issues` | Phase completed but with documented issues (used by `test_run`, `verify`) |
- **Fix:** Relabel the section to v3 and replace the body with the canonical phases-as-objects shape from forge-status-v3.schema.yml, or replace the inline block with a pointer to docs/schema/forge-status-v3.schema.yml. Remove or reconcile the top-level extension blocks and the `completed_with_known_issues` value, and add `not_applicable` to the phase-status comment.

### A-36 — forge.md writes four phase-status keys (i18n_harvest, migration_plan, monitoring_setup, experiment_design) that do not exist in the canonical phases map
- **Dimension:** schema · **Location:** `commands/forge.md:516, 581, 792, 820` · **Class:** drift (high confidence)
- **Problem:** forge.md instructs writing `i18n_harvest.status`, `migration_plan.status`, `monitoring_setup.status`, and `experiment_design.status` to `.forge-status.yml`, but none of these four keys exist in the canonical `phases:` map (forge-status-v3.schema.yml:67-116). This is drift rather than illustrative omission because schema.md:102 explicitly enumerates which extra phase keys are valid (`design_system_harvest` and `spec_merge`) and does NOT include these four. A reader treating the canonical schema as authoritative for the phase key-set would find these keys undocumented.
- **Evidence:** Update `.forge-status.yml`: `i18n_harvest.status = completed` (or `skipped` / `not_applicable` for English-only projects).
- **Fix:** Add `i18n_harvest`, `migration_plan`, `monitoring_setup`, and `experiment_design` to the canonical `phases:` map in forge-status-v3.schema.yml (and list them in schema.md:102 alongside design_system_harvest/spec_merge), or change forge.md to use existing canonical phase keys.

### A-37 — Unresolved sibling collision (same pattern as tasks[]→task_log[] seed): phases.api_docs vs top-level api_docs:
- **Dimension:** schema · **Location:** `commands/api-docs.md:485-487` · **Class:** drift (high confidence)
- **Problem:** api-docs.md writes BOTH a `phases.api_docs` status entry AND a sibling top-level `api_docs:` block under the same name on `.forge-status.yml`. This is exactly the `phases.tasks` ↔ top-level `tasks` name collision that the v3 schema resolved by renaming the array to `task_log[]` (schema.md:30-31), but here it was left unrenamed. security-check.md (phases.security_check + top-level `security:`) and tracking-plan.md (phases.tracking_plan + top-level `tracking:`) repeat the pattern. Additionally, neither the `phases.api_docs`/`phases.security_check`/`phases.tracking_plan` keys nor the top-level `api_docs:`/`security:`/`tracking:` blocks exist in the canonical v3 schema. file-structure.md:207-208 documents this collision as intended ("write completion status to phases: using their own keys api_docs, security_check, tracking_plan").
- **Evidence:** phases:   api_docs: completed api_docs:   endpoints: {N}
- **Fix:** Apply the same fix used for tasks→task_log: rename the top-level instrumentation blocks (e.g. api_docs→api_docs_report) to remove the sibling collision, and add the resulting phase keys + blocks to the canonical schema, or drop the duplicate phase key and track completion solely under the top-level block.

### A-38 — `phases.<phase>.produced_by` is required by the distinct-approver rule but is absent from the canonical schema
- **Dimension:** schema · **Location:** `docs/policy.md:281, 295` · **Class:** drift (high confidence)
- **Problem:** policy.md §5.3 defines an author-identity field `phases.<phase>.produced_by` written by the orchestrator at phase completion when `role_approvals.solo_mode: false`, and the distinct-approver constraint (approved_by != produced_by) depends on it. This field does not appear anywhere in the canonical forge-status-v3.schema.yml phase object (lines 66-116), which lists started_at/completed_at/tokens_in/tokens_out/tool_calls/digest_path/skipped/skip_reason but no produced_by. A reader validating against the canonical schema would treat produced_by as undocumented.
- **Evidence:** | Author | `phases.<phase>.produced_by` | identity (email/handle) of the human who owned the artifact this phase produced | written by the orchestrator at phase completion when `role_approvals.solo_mode: false` |
- **Fix:** Add `produced_by` to the per-phase field set in forge-status-v3.schema.yml (and to schema.md §5 field catalog), marked optional / required-when-solo_mode-false.

### A-39 — release-readiness writes gates[].action_items and release_readiness.supply_chain_paths fields absent from the canonical schema
- **Dimension:** schema · **Location:** `commands/release-readiness.md:507-512` · **Class:** drift (high confidence)
- **Problem:** release-readiness.md instructs writing an `action_items: {must, must_completed, should, nice_to_have}` sub-object on a `gates[]` entry (lines 508-512) and a `release_readiness.supply_chain_paths` block (line 366-367). Neither field exists in the canonical gates[] entry shape (forge-status-v3.schema.yml:147-172) nor anywhere else in the canonical schema. The gates[] decision enum itself is now correct (6 values, line 502), so this is a field-coverage gap, not enum drift.
- **Evidence:**     action_items:       must: {N}       must_completed: {N}       should: {N}       nice_to_have: {N}
- **Fix:** Either add `gates[].action_items` and `release_readiness.supply_chain_paths` to the canonical schema, or remove these writes from release-readiness.md so the gate entry conforms to the canonical shape.

### A-40 — Docs claim acquire-lock.sh uses `mv -n` with `ln` fallback, but the script uses `ln` only and explicitly rejects `mv -n`
- **Dimension:** scripts · **Location:** `docs/runtime.md:128` · **Class:** genuine_contradiction (high confidence)
- **Problem:** Two normative docs state that scripts/acquire-lock.sh acquires the lock using `mv -n` with an `ln` fallback. The actual script uses `ln` as the sole acquisition primitive and its own comment explicitly rejects `mv -n` as unsafe (BSD `mv -n` silently skips with exit 0, defeating the race guard). The doc description is the inverse of what the script does. An LLM/maintainer reading the doc would misunderstand the atomicity mechanism (e.g. might 'restore' a non-existent mv-n path or assume mv-n behavior).
- **Evidence:** runtime.md:128 `- \`scripts/acquire-lock.sh\` uses atomic primitives (\`mv -n\` with \`ln\` fallback)` ; how-it-works-v2.md:231 `- \`acquire-lock.sh\` uses \`mv -n\` with an \`ln\` fallback (both atomic on` ; CONTRADICTED BY acquire-lock.sh:82-84 `# Atomic acquisition via \`ln\`. Unlike BSD \`mv -n\` (which silently skips` `# on existing target with exit 0, defeating the race guard), \`ln\` exits` `# non-zero on existing target on both GNU and BSD` and acquire-lock.sh:93 `if ! ln "$tmp_lock" "$lock_path" 2>/dev/null; then`
- **Fix:** Update both runtime.md:128 and how-it-works-v2.md:231 to say the script uses `ln` (hardlink) for atomic acquisition with a session_id verification step, and remove the `mv -n` claim. The script behavior is correct; the docs are stale.

### A-41 — countFindings counts every F-NNN bullet regardless of Open/Acknowledged/Resolved state, despite claiming to count only OPEN findings
- **Dimension:** scripts · **Location:** `scripts/gate-risk.js:85-97` · **Class:** genuine_contradiction (medium confidence)
- **Problem:** The function comment states it counts OPEN CRITICAL/HIGH findings, but the implementation has no state filter: it counts any line containing `F-\d+` plus `CRITICAL`/`HIGH`. The gate-review.md template defines distinct Summary columns Open / Acknowledged / Resolved and instructs phases to move fixed findings to Resolved (keeping the `**F-NNN** · SEVERITY` bullet form). Acknowledged and Resolved findings therefore still carry an `F-NNN`+severity bullet and get counted as open, over-escalating the risk class in the PRIMARY gate-review.md path — which directly feeds the medium/high routing and the `--ci` gate.
- **Evidence:** gate-risk.js:85-86 `  // Count OPEN CRITICAL/HIGH findings, anchored to finding LINES so summary-table` ; gate-risk.js:94-96 `      if (!/\bF-\d+\b/.test(line)) continue;          // only finding bullets, not the legend` `      if (/\bCRITICAL\b/.test(line)) critical++;` `      else if (/\bHIGH\b/.test(line)) high++;` ; template gate-review.md:46 `| Severity | Open | Acknowledged | Resolved |` ; gate-review.md:99 `   sha, flag it "may be stale — re-confirm"; if fixed, move it to Resolved.`
- **Fix:** Scope counting to the Open section / Open-state bullets only (e.g. parse per-cohort 'Open' grouping or require an explicit `state: open` marker), or drop the word OPEN from the comment if all-state counting is intended. As written, code and comment disagree.

## LOW (15)

### A-42 — max_tokens_per_doc recommended-range prose drift between template and config.md
- **Dimension:** config-keys · **Location:** `config-template.yml:239` · **Class:** drift (high confidence)
- **Problem:** The actual default (4000) is consistent, but the recommended range differs between sources: config-template.yml says 3000-5000, while config.md says 3000 (concise) to 6000 (exhaustive). The guidance an LLM gives a user differs depending on which file it reads.
- **Evidence:** config-template.yml:239: "# Recommended: 3000-5000 tokens per file"  config.md:260: "Recommended range: `3000` (concise) to `6000` (exhaustive)."
- **Fix:** Pick one range (e.g. 3000-6000) and make config-template.yml line 239 and config.md line 260 match.

### A-43 — forge.md description lists only 3 modes (omits express) while the same file's validator and Express section treat express as first-class
- **Dimension:** contradictions · **Location:** `commands/forge.md:6` · **Class:** genuine_contradiction (high confidence)
- **Problem:** The forge.md front-matter description enumerates the feature modes as '(lite / standard / v-model)', omitting `express`, yet the same file validates the resolved mode as one of `express | lite | standard | v-model` (§Mode Resolution) and contains a dedicated '### Express mode' section. A reader of the command description would not know express is a supported mode of the orchestrator it heads.
- **Evidence:** OMITTING side — commands/forge.md:5-6 (front-matter description): "...the phase map selected by `feature_mode` / (lite / standard / v-model) with human-in-the-loop gates". INCLUDING side (same file) — commands/forge.md:279: "The resolved value MUST be one of `\"express\" | \"lite\" | \"standard\" | \"v-model\"`" and commands/forge.md:149 heading "### Express mode" with :151 "When `feature_mode: express`, run only `product-spec`...".
- **Fix:** Update commands/forge.md front-matter line 6 to '(express / lite / standard / v-model)' to match the file's own validator and Express mode section.

### A-44 — max_tokens_per_doc recommended range stated as 3000-5000 in template but 3000-6000 in config.md
- **Dimension:** contradictions · **Location:** `config-template.yml:239` · **Class:** drift (high confidence)
- **Problem:** The recommended range for `max_tokens_per_doc` differs between the config template comment and the config reference doc. Minor, but the two documents that a user consults to set the same key give different guidance.
- **Evidence:** config-template.yml:239: "# Recommended: 3000-5000 tokens per file" vs docs/config.md:260: "Recommended range: `3000` (concise) to `6000` (exhaustive)."
- **Fix:** Align the two recommended ranges (e.g. make both 3000-6000, matching config.md which also documents the 8000 ceiling).

### A-45 — how-it-works-v2 file tree says '29 slash-command definitions' — actually 31
- **Dimension:** counts-versions · **Location:** `docs/how-it-works-v2.md:30` · **Class:** drift (high confidence)
- **Problem:** The annotated file tree labels commands/ as containing 29 slash-command definitions, but it contains 31 .md files. Stale by 2 (the v1.6 additions design-system-harvest and spec-merge).
- **Evidence:** ├── commands/                    # 29 slash-command definitions
- **Fix:** Update '29' to '31'.

### A-46 — QA plan intro says '29 of them are slash-command definitions' — actually 31
- **Dimension:** counts-versions · **Location:** `docs/qa/plugin-test-plan.md:19` · **Class:** drift (high confidence)
- **Problem:** A second 29-count claim in the same QA doc's intro. Repo has 31 command files. Compounds numdrift-1.
- **Evidence:** ships ~50 files total. 29 of them are slash-command definitions in
- **Fix:** Update '29' to '31' (and optionally re-verify the '~50 files total' approximation).

### A-47 — README headline banner says 'New in v1.5.0' with zero v1.6 content despite 1.6.0 release
- **Dimension:** counts-versions · **Location:** `README.md:12` · **Class:** drift (high confidence)
- **Problem:** The README's top 'New in' banner advertises v1.5.0 only and contains no v1.6 mention anywhere in the file (verified: grep for v1.6/1.6.0 in README returns nothing), while extension.yml = 1.6.0 and docs/how-it-works-v2.md/phases.md/config-template.yml all describe a v1.6 release. README is the primary end-user doc and lags a full minor version.
- **Evidence:** **New in v1.5.0:**
- **Fix:** Add a 'New in v1.6.0' section summarizing the v1.6 wave above/replacing the v1.5.0 banner.

### A-48 — README 'Install (specific version)' pins v1.5.1, behind shipped 1.6.0
- **Dimension:** counts-versions · **Location:** `README.md:396` · **Class:** drift (high confidence)
- **Problem:** The install-by-version example pins the v1.5.1 release tag, which no longer matches the current extension.yml version 1.6.0. A user following this 'specific version' example installs an older release than the repo represents.
- **Evidence:** specify extension add product-forge --from https://github.com/VaiYav/speckit-product-forge/archive/refs/tags/v1.5.1.zip
- **Fix:** Bump the pinned tag to v1.6.0 (once the 1.6.0 tag/CHANGELOG entry exists) to match extension.yml.

### A-49 — SEC-NNN security-finding ID family is missing from the naming-convention registry
- **Dimension:** naming-ids · **Location:** `commands/security-check.md:246` · **Class:** drift (high confidence)
- **Problem:** security-check.md defines and uses SEC-NNN as a canonical security-finding ID family (SEC-001..SEC-004), and even instructs the user to create tracker tasks referencing them, but SEC- has no row in the naming-convention registry. It is a distinct family from the registered R-NNN (risk), D-NNN (design), A-NNN (arch), and REV-NNN (code-review finding) IDs, so it needs its own entry.
- **Evidence:** security-check.md:246 "#### SEC-001: {Title}", security-check.md:263 "#### SEC-002: {Title}", security-check.md:346 "> Create tasks in your tracker for: SEC-001, SEC-002, ...". No `SEC-` row in file-structure.md:375-398.
- **Fix:** Add a registry row: `| Security finding IDs | SEC-NNN | SEC-001, SEC-004 |`.

### A-50 — F-NNN unified gate-finding ID family is missing from the registry though forge.md greps for it
- **Dimension:** naming-ids · **Location:** `commands/forge.md:231` · **Class:** drift (high confidence)
- **Problem:** gate-review.md uses F-NNN as the unified gate-finding ID family (F-001..F-00N), and forge.md's auto-recommend pre-gate depends on it via a literal grep for `^- \*\*F-[0-9]+\*\*.*CRITICAL`. F- is a distinct, load-bearing ID family but has no row in the naming-convention registry; its single-letter prefix is also easy to confuse with the registered FR- family.
- **Evidence:** forge.md:232 "grep -E '^- \*\*F-[0-9]+\*\*.*CRITICAL' {FEATURE_DIR}/gate-review.md"; gate-review.md:63 "- **F-001** · ❌ CRITICAL · `code-review/security` · ...", gate-review.md:64 "- **F-002** · 🔸 MEDIUM ...". No `F-` row in file-structure.md:375-398.
- **Fix:** Add a registry row: `| Unified gate-finding IDs | F-NNN | F-001, F-042 |`, and note it is consumed by forge.md's auto-recommend pre-gate grep so the exact `**F-NNN**` bullet format must be preserved.

### A-51 — Orphan-task reconciliation uses substring matching that can false-match non-zero-padded short task IDs
- **Dimension:** naming-ids · **Location:** `scripts/validate-traceability.js:180` · **Class:** real_bug (medium confidence)
- **Problem:** checkTasksMd reconciles task IDs between tasks.md and the traceability matrix using bidirectional `.includes()` substring matching after normalizing TASK->T and stripping hyphens. With substring matching, a short id like `T1` would be treated as referenced if any longer id such as `T12`/`T100` exists in the matrix, suppressing a legitimate orphan-task warning. The zero-padded `T001` convention used everywhere else prevents this in practice, so it is a latent bug rather than an active one; the regex at line 175 also only matches `T-?digits`, so a tasks.md authored with the matrix's own `TASK-NNN` form would not be detected at all.
- **Evidence:** validate-traceability.js:180 "const referenced = [...seenTasks].some((t) => t.replace(\"-\", \"\").replace(/^TASK/, \"T\").includes(norm) || norm.includes(t.replace(\"-\", \"\").replace(/^TASK/, \"T\")));"; line 175 "const re = /^\s*-\s*\[[ xX]\]\s*(T-?\d{1,4})\b/gm;".
- **Fix:** Compare normalized task IDs for exact equality (after a single, consistent normalization of both sides to `T` + zero-stripped/zero-padded number) instead of `.includes()`, and broaden the tasks.md regex to also accept the `TASK-NNN` form so all documented task-ID variants are detected.

### A-52 — how-it-works §4 feature-modes table omits express mode
- **Dimension:** phase-map · **Location:** `docs/how-it-works-v2.md:100-104` · **Class:** drift (high confidence)
- **Problem:** how-it-works-v2.md §4 "Feature modes" is the maintainer-facing mode reference and its table lists only lite/standard/v-model. express is never mentioned anywhere in how-it-works-v2.md, despite express being a first-class mode validated by forge.md (L278-281) and given a phase map in policy.md §4.1 and forge.md's "Express mode" section (L149-155).
- **Evidence:** how-it-works-v2.md L100-104: "| Mode | Phase count | Use case |\n| `lite` | 5 | ... |\n| `standard` | 14 (+ 4 opt) | Default — full lifecycle |\n| `v-model` | standard + IEEE artifacts | ... |" — no `express` row; grep for "express" in the file returns nothing.
- **Fix:** Add an `express` row to the §4 modes table (phase count 4: product-spec→plan→implement→verify; use case: trivial copy/config/one-liner changes) so the maintainer reference matches the implemented mode set.

### A-53 — how-it-works §5 "18-phase" standard list omits 2H (design-system-harvest) and Phase 10 (spec-merge)
- **Dimension:** phase-map · **Location:** `docs/how-it-works-v2.md:131-155` · **Class:** drift (high confidence)
- **Problem:** how-it-works §5 is titled "The phase map (standard mode)" and claims a "Full 18-phase flow", but its list (L137-154) enumerates 0,1,2,3,4,4.5,5,5B,5.5,5C,6,6B,7,8A,8B,9,9.5,9B and omits the 2H Design System Harvest helper and Phase 10 Spec Merge that forge.md's standard Phase Map table (L43, L59) includes. The §5 list is therefore not the full standard map forge.md defines.
- **Evidence:** how-it-works-v2.md L133-134: "Full 18-phase flow. Opt = optional; Cond = conditional ..."; the code block L137-154 ends at "9B experiment-design (opt, cond: flag marked experiment: true)" with no design-system-harvest or spec-merge entry, whereas forge.md L43/L59 list "2H. Design System Harvest" and "10. Spec Merge".
- **Fix:** Add 2H (design-system-harvest, UI features) and 10 (spec-merge, post-release living spec) to the §5 flow list, or note explicitly that 2H is a Phase-2 helper and Phase 10 is post-9 so the list reconciles with forge.md's Phase Map table.

### A-54 — Journey-level test-coverage check is silently skipped whenever the journey has any edges
- **Dimension:** scripts · **Location:** `scripts/validate-traceability.js:138` · **Class:** drift (high confidence)
- **Problem:** The journey-level missing-test error only fires when `edges.length === 0`. A journey that HAS edges but has an empty journey-level `tests[]` never produces a `journey.tests` error, even after test_run is completed. This contradicts the script's own header contract ('every Must-Have journey + P0/P1 edge has a test') and the inline comment ('every journey -> >=1 test'). Empirically verified: a journey with `tests: []` and one edge produces 0 errors. Happy-path (journey-level) coverage gaps go unreported.
- **Evidence:** validate-traceability.js:130 `  // Journeys: every journey -> >=1 test; every P0/P1 edge -> >=1 test.` ; validate-traceability.js:138 `    if (testDone && !jTests && edges.length === 0) {` ; header contract line 11-12 `// null link past the phase that owns it; every Must-Have journey + P0/P1 edge` `// has a test;`
- **Fix:** Remove the `&& edges.length === 0` guard so a journey with an empty `tests[]` is flagged once test_run is complete regardless of whether it has edges; edge coverage is checked separately in the loop below. Optionally honor a journey-level `must_have` flag to avoid flagging nice-to-have journeys.

### A-55 — Orphan-task detection uses bidirectional substring matching, producing false negatives for prefix-related task ids
- **Dimension:** scripts · **Location:** `scripts/validate-traceability.js:178-181` · **Class:** real_bug (high confidence)
- **Problem:** The orphan check considers a tasks.md id 'referenced' if either id is a substring of the other after normalization. So `T1` is treated as referenced by `TASK-10`, and `T12` is treated as referenced by `TASK-1`. Mixed-width task ids defeat the check and a genuine orphan task is silently not flagged. Verified empirically: `T1` vs only `TASK-10` returns referenced=true.
- **Evidence:** validate-traceability.js:180 `    const referenced = [...seenTasks].some((t) => t.replace("-", "").replace(/^TASK/, "T").includes(norm) || norm.includes(t.replace("-", "").replace(/^TASK/, "T")));`
- **Fix:** Normalize both ids to a canonical form (strip prefix, zero-pad the numeric part to a fixed width) and compare for EQUALITY rather than bidirectional `.includes()`.

### A-56 — session_id is interpolated unescaped into the JSON lock payload and into a grep pattern
- **Dimension:** scripts · **Location:** `scripts/acquire-lock.sh:90` · **Class:** real_bug (medium confidence)
- **Problem:** session_id (from arg or $FORGE_SESSION_ID) is interpolated raw into the JSON heredoc payload and later into a grep regex used to verify ownership. A session_id containing a double-quote would produce malformed JSON, and one containing regex metacharacters could break or alter the verification grep at line 102 (and the release-lock grep). Inputs are normally controlled, so impact is low, but it is unvalidated input at a system boundary.
- **Evidence:** acquire-lock.sh:90 `{"pid": $$, "session_id": "$session_id", "acquired_at": "$now_iso", "ttl_seconds": $ttl_seconds}` ; acquire-lock.sh:102 `if ! grep -q "\"session_id\"[[:space:]]*:[[:space:]]*\"$session_id\"" "$lock_path" 2>/dev/null; then`
- **Fix:** Reject or sanitize session_id (allow only [A-Za-z0-9._-]) before use, or use grep -F with a fixed string for the verification step.

## Completeness critic

**Dimensions not covered by the audit pass (recommended follow-ups):**
- release-version consistency across CHANGELOG.md <-> README.md <-> extension.yml <-> docs/* <-> docs/qa/plugin-test-plan.md: the tree is intentionally at v1.6.0 (extension.yml:6 'version: "1.6.0"'; docs/interaction.md:3, docs/journeys.md:3, docs/phases.md:1, docs/file-structure.md:25 all stamp 'v1.6'), yet CHANGELOG.md tops out at '## [1.5.1]' with NO 1.6.0 entry, README install pins the v1.5.1 zip, extension.yml:7 description still opens 'v1.5 adds...', and docs/qa/plugin-test-plan.md:1 is titled 'v1.5.0'. No audited dimension verified release-label coherence end-to-end.
- template <-> consumer CONTRACT correctness: whether a docs/templates/* artifact's documented shape actually satisfies the script/command that PARSES it. The 'scripts' dimension read each .js in isolation and 'xref' (0 findings) clearly scanned only commands/, so the gate-review.md format <-> gate-risk.js countFindings() parser seam was never checked together. This is where the highest-severity defect lives (see additional_findings).
- QA / self-test-plan COVERAGE completeness: whether docs/qa/plugin-test-plan.md still enumerates the real command set (31) and the real script set (6 executables). It is stale at 29 commands / 4 helpers and omits 3 commands and 3 scripts. No dimension audited the test plan as an inventory artifact.
- xref coverage of docs/templates/ and the docs/improvements/* set: 'xref: 0 findings' is strong evidence the xref pass never opened the template files or the improvement memos; their relative links and #anchors went unverified (I verified the live ones resolve, but the dimension itself did not run there).
- CHANGELOG completeness vs shipped surface: the entire Wave-5 feature set now present in the tree (scripts/gate-risk.js, scripts/validate-traceability.js, scripts/lib-yaml.js, docs/templates/gate-policy.yml, docs/templates/gate-review.md, docs/templates/traceability-matrix.md, the interaction convention, structured journeys) has ZERO mentions in CHANGELOG.md (grep count 0). A 'does the changelog document what shipped' check was absent.

**Files likely not opened by any dimension:**

`docs/templates/gate-policy.yml`, `docs/templates/gate-review.md`, `docs/templates/traceability-matrix.md`, `docs/templates/phase-digest.md`, `docs/templates/portfolio-report.md`, `docs/templates/interaction-prompts.md`, `docs/templates/journey-spec.md`, `docs/qa/plugin-test-plan.md`, `docs/interaction.md`, `docs/journeys.md`, `docs/testing-strategy.md`, `docs/v-model-integration.md`, `docs/lessons-format.md`, `docs/schema/migration-v2-to-v3.md`, `docs/improvements/2026-05-sdd-flow-improvements.md`, `docs/improvements/2026-05-storage-strategy-and-global-config.md`, `docs/improvements/2026-05-wave5-roadmap.md`, `scripts/validate-traceability.js`, `scripts/lib-yaml.js`, `scripts/gate-risk.js`

**Additional findings surfaced by the critic (unverified — confirm before fixing):**

| Severity | File | Finding |
|----------|------|---------|
| HIGH | `/Users/valentinyakovlev/projects/speckit-extension/speckit-product-forge/scripts/gate-risk.js:85-98` | gate-risk.js counts RESOLVED/ACKNOWLEDGED findings as OPEN, breaking the no_open_critical CI pre-gate: countFindings() comment (line 86) states 'Count OPEN CRITICAL/HIGH findings', and gate-policy.yml:28 enforces 'no_open_critical: true # gate-review.md has zero open CRITICAL F-NNN'. But the parser (lines 93-97) increments for EVERY line where /\bF-\d+\b/ AND /\bCRITICAL\b/ (or /\bHIGH\b/) match, with ZERO finding-state filtering. The gate-review.md template (line 99) directs 'if fixed, move it to Resolved' while mandating 'Do not renumber existing F-NNN' / 'ids are stable once assigned' (lines 32, 97) and provides NO inline resolved/acknowledged marker on finding bullets (bullet format line 63: '- **F-001** · ❌ CRITICAL · ... → {fix}'). So a fixed CRITICAL whose bullet still contains the word CRITICAL is miscounted as open. Effect: gate-risk classifies risk too high AND validate-traceability/require_clean's no_open_critical pre-gate stays failed forever, so forge --ci's auto-recommend path is permanently blocked even after every CRITICAL is resolved. Cross-file root cause: the gate-review.md template never defines a resolved/ack inline format, and the parser never filters on one (scripts/gate-risk.js + docs/templates/gate-review.md + docs/templates/gate-policy.yml). |
| MEDIUM | `/Users/valentinyakovlev/projects/speckit-extension/speckit-product-forge/docs/qa/plugin-test-plan.md:19,77-83` | docs/qa/plugin-test-plan.md is stale: wrong command count and omits 3 commands: Line 19 says 'ships ~50 files total. 29 of them are slash-command definitions' and line 77 says '**29 slash commands**', but commands/*.md contains 31 files (matching extension.yml's 31 'provides.commands'). The enumerated list (lines 77-83) is missing design-system-harvest, revalidate, and spec-merge. A QA agent following this plan would under-test the plugin and never validate three real commands. |
| MEDIUM | `/Users/valentinyakovlev/projects/speckit-extension/speckit-product-forge/docs/qa/plugin-test-plan.md:22-26,91-92` | docs/qa/plugin-test-plan.md undercounts executable scripts and omits the 3 Wave-5 helpers: Lines 22-26 assert 'Only those 4 files are directly executable' and list migrate-status-v2-to-v3.js, acquire-lock.sh, release-lock.sh, migrate-status-v2-to-v3.ts. scripts/ actually holds 6 executables; gate-risk.js, validate-traceability.js, and lib-yaml.js are never mentioned anywhere in the plan (grep count 0). These three have --selftest entry points that a static/structural QA pass should run, so the test plan's 'static' mode coverage is incomplete. |
| MEDIUM | `/Users/valentinyakovlev/projects/speckit-extension/speckit-product-forge/extension.yml:131` | extension.yml sync-verify description says '7-layer' while every other source says '9-layer': extension.yml:131 describes sync-verify as a '7-layer artifact consistency checker'. commands/sync-verify.md:29 ('full 9-layer scan'), README.md:70 & 261, docs/how-it-works-v2.md:159, and docs/runtime.md:235 all say 9-layer. The plugin manifest description (user-facing in the command catalogue) is stale by two layers; an LLM reading the manifest would mis-describe the command's scope. |
| MEDIUM | `/Users/valentinyakovlev/projects/speckit-extension/speckit-product-forge/extension.yml:6-7` | extension.yml description and version disagree; CHANGELOG has no 1.6.0 entry: extension.yml:6 declares version '1.6.0' but the same block's description (line 7) still says 'v1.5 adds portfolio view, lite mode, ...' and never mentions any 1.6 feature. CHANGELOG.md's newest header is '## [1.5.1] — 2026-04-24' (no 1.6.0), README.md:396/408 still pin the v1.5.1 install/update zip, and docs/qa/plugin-test-plan.md:1 says v1.5.0 — while docs/interaction.md:3 and docs/journeys.md:3 are stamped 'normative for v1.6.0+' and docs/phases.md:1 / docs/file-structure.md:25 say v1.6. Consolidated release-label drift: a user installing per the README gets a v1.5.1 artifact that does not match the v1.6.0 docs in the tree. |
| LOW | `/Users/valentinyakovlev/projects/speckit-extension/speckit-product-forge/docs/testing-strategy.md:3` | docs/testing-strategy.md status header lags its own body: Line 3 stamps '> **Status:** normative for v1.5+' but the body documents v1.6 (W5) features as normative: line 144 'automated accessibility AA floor (v1.6, W5-B2)', line 158 'runtime component + design-token conformance (v1.6, W5-B5)', line 409 references both again. The status label should read v1.6+ to match docs/interaction.md:3 and docs/journeys.md:3. |

## Appendix — reviewed but not actionable

These candidates were raised by a finder but the verifier classified them as defensible imprecision or false positives (kept for transparency).

| File | Title | Class | Verifier note |
|------|-------|-------|---------------|
| `commands/test-plan.md` | UI-NNN and MIG-NNN appear as concrete 'Covers' references in a normative template but are undefined in the registry | defensible_imprecision | Confirmed verbatim: UI-012 in TC-UNIT-005 Covers cell (:300) and MIG-003 in TC-INT-004 Covers cell (:347); neither is in the registry. But the surrounding text defines Covers as 'story ID, functional requirement ID, or ADR ID' (:309) and 'requirement / NFR / migration ID' (:357), so UI-/MIG- read as illustrative placeholders for caller-chosen IDs, not sanctioned plugin families. Reconciles on a fair reading; a clarity nit at most. |
| `docs/policy.md` | "14-phase lifecycle documented in commands/forge.md" is an orphan count — forge.md never displays 14 | defensible_imprecision | Claim is literally true (grep: '14' in forge.md appears only as '≥14 days' L897), but '14' is not an orphan: forge.md frontmatter L8-9 lists exactly 13 explicit standard phases (research->release-readiness) and Phase 0 problem-discovery is a standard-mode phase (execution map L319), giving 13+1=14. The count is corpus-canonical (config.md L313 '14-phase'; how-it-works §4 '14 (+4 opt)'). forge.md IS the lifecycle doc; it just does not print the literal label. Reconciles on a fair reading; keep as a clarity note only. |
| `scripts/gate-risk.js` | Legacy fallback never counts verify-report.md WARNING findings toward HIGH risk | defensible_imprecision | Legacy fallback counts only CRITICAL-NNN from verify-report.md, never WARNING-NNN toward high. But WARNING is an explicitly advisory tier (verify-full.md:561 'Should Review', :567 'optional fix or acknowledge', :646 'acknowledged but not fixed'), not HIGH-equivalent; mapping WARNING→high would over-escalate just as easily. Omission is a defensible design choice, not a clear bug. |
| `scripts/validate-traceability.js` | statusInline phase-completion regex accepts `tested`/`verified` but the nested statusLine regex does not (asymmetry) | defensible_imprecision | statusInline regex accepts tested|verified but the v3 phase-status enum is pending|in_progress|completed|skipped|not_applicable (+approved, completed_with_known_issues). Those extra values are provably dead for phase statuses, so no LLM is misled. Inconsistency, not a defect. |
| `scripts/validate-traceability.js` | NFR table separator-row skip regex misses alignment-colon separators (`|:---|`) | false_positive | Claim's 'false negative' label is wrong: a colon separator |:---| yields cells[2]=':---', which is non-empty and not a placeholder, so it produces NO spurious error and misses NO real NFR — benign. Finding also admits no live trigger (bridge.md:218 uses plain dashes). |
| `scripts/lib-yaml.js` | Unquoted all-digit scalars are coerced to integers, dropping leading zeros (e.g. commit_sha: 0012 -> 12) | defensible_imprecision | Standard YAML scalar typing: unquoted all-digit scalars become ints. The v3 schema quotes SHAs (e.g. commit_sha: "abc1234"), so not triggered in practice. Correct behavior; worth at most a doc note, not a fix. |
